# ✅ Исправление backtest для работы с T-Invest API

## 🔧 Внесенные изменения

### 1. Обновлен метод `get_historical_data()` в `tinvest_api.py`

**Изменения:**
- Использует правильные даты для 2024 года при запросе исторических данных
- Разбивает запросы на части (максимум 365 дней для дневных свечей)
- Правильно обрабатывает преобразование времени свечей в datetime
- Улучшена обработка ошибок

**Особенности:**
- Для периода '2024' или '1y' используется весь 2024 год (01.01.2024 - 31.12.2024)
- Если текущая дата раньше конца 2024 года, используется текущая дата как конечная
- Для больших периодов делаются множественные запросы по 365 дней

### 2. Обновлен `backtest.py`

**Изменения:**
- Проверка токена на наличие реального значения (не пример)
- Правильная передача периода '2024' в T-Invest API
- Улучшены сообщения об ошибках

**Логика работы:**
1. Использует T‑Invest API (если токен настроен и установлен официальный SDK)
2. Если T‑Invest недоступен — backtest завершается с понятной ошибкой (без Yahoo/yfinance)

## 📋 Как использовать

### Для российских акций (через T-Invest API):

1. Убедитесь, что в `.env` указан реальный `TINVEST_TOKEN`:
   ```env
   TINVEST_TOKEN=t.ваш_реальный_токен
   TINVEST_SANDBOX=true
   BROKER=tinvest
   ```

2. Запустите:
   ```bash
   python backtest.py
   ```

3. Бот автоматически:
   - Получит данные за 2024 год через T-Invest API
   - Протестирует стратегию на всех символах из SYMBOLS
   - Покажет результаты

### Американские акции / yfinance

В этой сборке отключены. Бот ориентирован на рынок РФ и работает только через T‑Invest API.

## ⚙️ Технические детали

### Получение данных через T-Invest API

1. **Поиск инструмента:** Поиск FIGI по тикеру через `instruments.shares()`
2. **Получение свечей:** Использование `market_data.get_candles()` с правильными датами
3. **Обработка периодов:** Разбиение больших периодов на части по 365 дней
4. **Преобразование:** Конвертация protobuf сообщений в pandas DataFrame

### Формат данных

Результат - pandas DataFrame с колонками:
- `Open`, `High`, `Low`, `Close` - цены
- `Volume` - объем
- Индекс - `Time` (datetime)

## ⚠️ Важно

- Убедитесь, что `TINVEST_TOKEN` в `.env` содержит реальный токен (не `your_token_here`)
- Для работы в песочнице установите `TINVEST_SANDBOX=true`
- Для работы на реальной бирже установите `TINVEST_SANDBOX=false`

## 🎯 Результат

После исправлений backtest будет:
- ✅ Получать данные за 2024 год через T-Invest API
- ✅ Корректно обрабатывать все символы
- ✅ Показывать результаты тестирования стратегии
- ✅ Работать с российскими акциями (без Yahoo/yfinance)

Все исправления применены и готовы к использованию! 🚀
