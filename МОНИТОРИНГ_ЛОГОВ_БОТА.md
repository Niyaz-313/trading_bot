# МОНИТОРИНГ ЛОГОВ БОТА В РЕАЛЬНОМ ВРЕМЕНИ

## 🔍 Что вы видите

То, что вы видите - это просто **просмотр содержимого файла** через `less` или `cat`.

**Чтобы выйти:**
- Нажмите `q` (quit - выйти)
- Или `Ctrl+C`

---

## ✅ КАК СЛЕДИТЬ ЗА ЛОГАМИ В ОНЛАЙН РЕЖИМЕ

### Способ 1: Просмотр логов через journalctl (рекомендуется)

```bash
# Просмотр логов в реальном времени
sudo journalctl -u trading-bot.service -f

# Просмотр последних 50 строк и затем в реальном времени
sudo journalctl -u trading-bot.service -n 50 -f

# Просмотр логов с определенного времени
sudo journalctl -u trading-bot.service --since "10 minutes ago" -f
```

**Для выхода:** Нажмите `Ctrl+C`

---

### Способ 2: Просмотр логов через tail

```bash
# Просмотр логов в реальном времени
tail -f /home/botuser/trading_bot/trading_bot/logs/trading_bot.log

# Просмотр логов ошибок в реальном времени
tail -f /home/botuser/trading_bot/trading_bot/logs/bot_error.log

# Просмотр нескольких файлов одновременно
tail -f /home/botuser/trading_bot/trading_bot/logs/trading_bot.log /home/botuser/trading_bot/trading_bot/logs/bot_error.log
```

**Для выхода:** Нажмите `Ctrl+C`

---

### Способ 3: Просмотр аудита в реальном времени

```bash
# Просмотр аудита в реальном времени
tail -f /home/botuser/trading_bot/trading_bot/audit_logs/trades_audit.jsonl
```

---

## 📋 ПОЛЕЗНЫЕ КОМАНДЫ ДЛЯ МОНИТОРИНГА

### Проверка статуса сервиса

```bash
# Статус сервиса
sudo systemctl status trading-bot.service

# Статус с подробностями
sudo systemctl status trading-bot.service -l

# Статус без пагинации
sudo systemctl status trading-bot.service --no-pager
```

### Просмотр последних логов

```bash
# Последние 100 строк
sudo journalctl -u trading-bot.service -n 100

# Последние 50 строк без пагинации
sudo journalctl -u trading-bot.service -n 50 --no-pager

# Логи за последний час
sudo journalctl -u trading-bot.service --since "1 hour ago"
```

### Поиск в логах

```bash
# Поиск ошибок
sudo journalctl -u trading-bot.service | grep -i error

# Поиск конкретного символа
sudo journalctl -u trading-bot.service | grep "SBER"

# Поиск с контекстом (5 строк до и после)
sudo journalctl -u trading-bot.service | grep -i error -A 5 -B 5
```

---

## 🚀 БЫСТРЫЕ КОМАНДЫ

### Просмотр логов в реальном времени (одна команда)

```bash
sudo journalctl -u trading-bot.service -f
```

### Просмотр последних 50 строк и затем в реальном времени

```bash
sudo journalctl -u trading-bot.service -n 50 -f
```

### Просмотр логов файла в реальном времени

```bash
tail -f /home/botuser/trading_bot/trading_bot/logs/trading_bot.log
```

---

## ⚠️ ВАЖНО

1. **Для выхода из просмотра:** Нажмите `Ctrl+C` или `q`
2. **`-f` означает "follow"** - следить за файлом в реальном времени
3. **`-n` означает количество строк** - показать последние N строк
4. **`journalctl`** - просмотр логов systemd сервиса
5. **`tail -f`** - просмотр логов файла в реальном времени

---

## 📊 МОНИТОРИНГ РАЗНЫХ ЛОГОВ

### Все логи сервиса

```bash
sudo journalctl -u trading-bot.service -f
```

### Только ошибки

```bash
sudo journalctl -u trading-bot.service -p err -f
```

### Логи с временными метками

```bash
sudo journalctl -u trading-bot.service -f --since "today"
```

### Логи файла бота

```bash
tail -f /home/botuser/trading_bot/trading_bot/logs/trading_bot.log
```

### Логи ошибок

```bash
tail -f /home/botuser/trading_bot/trading_bot/logs/bot_error.log
```

### Аудит сделок

```bash
tail -f /home/botuser/trading_bot/trading_bot/audit_logs/trades_audit.jsonl
```

---

## ✅ РЕКОМЕНДУЕМАЯ КОМАНДА

**Для постоянного мониторинга используйте:**

```bash
sudo journalctl -u trading-bot.service -f
```

Эта команда показывает все логи сервиса в реальном времени.


## 🔍 Что вы видите

То, что вы видите - это просто **просмотр содержимого файла** через `less` или `cat`.

**Чтобы выйти:**
- Нажмите `q` (quit - выйти)
- Или `Ctrl+C`

---

## ✅ КАК СЛЕДИТЬ ЗА ЛОГАМИ В ОНЛАЙН РЕЖИМЕ

### Способ 1: Просмотр логов через journalctl (рекомендуется)

```bash
# Просмотр логов в реальном времени
sudo journalctl -u trading-bot.service -f

# Просмотр последних 50 строк и затем в реальном времени
sudo journalctl -u trading-bot.service -n 50 -f

# Просмотр логов с определенного времени
sudo journalctl -u trading-bot.service --since "10 minutes ago" -f
```

**Для выхода:** Нажмите `Ctrl+C`

---

### Способ 2: Просмотр логов через tail

```bash
# Просмотр логов в реальном времени
tail -f /home/botuser/trading_bot/trading_bot/logs/trading_bot.log

# Просмотр логов ошибок в реальном времени
tail -f /home/botuser/trading_bot/trading_bot/logs/bot_error.log

# Просмотр нескольких файлов одновременно
tail -f /home/botuser/trading_bot/trading_bot/logs/trading_bot.log /home/botuser/trading_bot/trading_bot/logs/bot_error.log
```

**Для выхода:** Нажмите `Ctrl+C`

---

### Способ 3: Просмотр аудита в реальном времени

```bash
# Просмотр аудита в реальном времени
tail -f /home/botuser/trading_bot/trading_bot/audit_logs/trades_audit.jsonl
```

---

## 📋 ПОЛЕЗНЫЕ КОМАНДЫ ДЛЯ МОНИТОРИНГА

### Проверка статуса сервиса

```bash
# Статус сервиса
sudo systemctl status trading-bot.service

# Статус с подробностями
sudo systemctl status trading-bot.service -l

# Статус без пагинации
sudo systemctl status trading-bot.service --no-pager
```

### Просмотр последних логов

```bash
# Последние 100 строк
sudo journalctl -u trading-bot.service -n 100

# Последние 50 строк без пагинации
sudo journalctl -u trading-bot.service -n 50 --no-pager

# Логи за последний час
sudo journalctl -u trading-bot.service --since "1 hour ago"
```

### Поиск в логах

```bash
# Поиск ошибок
sudo journalctl -u trading-bot.service | grep -i error

# Поиск конкретного символа
sudo journalctl -u trading-bot.service | grep "SBER"

# Поиск с контекстом (5 строк до и после)
sudo journalctl -u trading-bot.service | grep -i error -A 5 -B 5
```

---

## 🚀 БЫСТРЫЕ КОМАНДЫ

### Просмотр логов в реальном времени (одна команда)

```bash
sudo journalctl -u trading-bot.service -f
```

### Просмотр последних 50 строк и затем в реальном времени

```bash
sudo journalctl -u trading-bot.service -n 50 -f
```

### Просмотр логов файла в реальном времени

```bash
tail -f /home/botuser/trading_bot/trading_bot/logs/trading_bot.log
```

---

## ⚠️ ВАЖНО

1. **Для выхода из просмотра:** Нажмите `Ctrl+C` или `q`
2. **`-f` означает "follow"** - следить за файлом в реальном времени
3. **`-n` означает количество строк** - показать последние N строк
4. **`journalctl`** - просмотр логов systemd сервиса
5. **`tail -f`** - просмотр логов файла в реальном времени

---

## 📊 МОНИТОРИНГ РАЗНЫХ ЛОГОВ

### Все логи сервиса

```bash
sudo journalctl -u trading-bot.service -f
```

### Только ошибки

```bash
sudo journalctl -u trading-bot.service -p err -f
```

### Логи с временными метками

```bash
sudo journalctl -u trading-bot.service -f --since "today"
```

### Логи файла бота

```bash
tail -f /home/botuser/trading_bot/trading_bot/logs/trading_bot.log
```

### Логи ошибок

```bash
tail -f /home/botuser/trading_bot/trading_bot/logs/bot_error.log
```

### Аудит сделок

```bash
tail -f /home/botuser/trading_bot/trading_bot/audit_logs/trades_audit.jsonl
```

---

## ✅ РЕКОМЕНДУЕМАЯ КОМАНДА

**Для постоянного мониторинга используйте:**

```bash
sudo journalctl -u trading-bot.service -f
```

Эта команда показывает все логи сервиса в реальном времени.


## 🔍 Что вы видите

То, что вы видите - это просто **просмотр содержимого файла** через `less` или `cat`.

**Чтобы выйти:**
- Нажмите `q` (quit - выйти)
- Или `Ctrl+C`

---

## ✅ КАК СЛЕДИТЬ ЗА ЛОГАМИ В ОНЛАЙН РЕЖИМЕ

### Способ 1: Просмотр логов через journalctl (рекомендуется)

```bash
# Просмотр логов в реальном времени
sudo journalctl -u trading-bot.service -f

# Просмотр последних 50 строк и затем в реальном времени
sudo journalctl -u trading-bot.service -n 50 -f

# Просмотр логов с определенного времени
sudo journalctl -u trading-bot.service --since "10 minutes ago" -f
```

**Для выхода:** Нажмите `Ctrl+C`

---

### Способ 2: Просмотр логов через tail

```bash
# Просмотр логов в реальном времени
tail -f /home/botuser/trading_bot/trading_bot/logs/trading_bot.log

# Просмотр логов ошибок в реальном времени
tail -f /home/botuser/trading_bot/trading_bot/logs/bot_error.log

# Просмотр нескольких файлов одновременно
tail -f /home/botuser/trading_bot/trading_bot/logs/trading_bot.log /home/botuser/trading_bot/trading_bot/logs/bot_error.log
```

**Для выхода:** Нажмите `Ctrl+C`

---

### Способ 3: Просмотр аудита в реальном времени

```bash
# Просмотр аудита в реальном времени
tail -f /home/botuser/trading_bot/trading_bot/audit_logs/trades_audit.jsonl
```

---

## 📋 ПОЛЕЗНЫЕ КОМАНДЫ ДЛЯ МОНИТОРИНГА

### Проверка статуса сервиса

```bash
# Статус сервиса
sudo systemctl status trading-bot.service

# Статус с подробностями
sudo systemctl status trading-bot.service -l

# Статус без пагинации
sudo systemctl status trading-bot.service --no-pager
```

### Просмотр последних логов

```bash
# Последние 100 строк
sudo journalctl -u trading-bot.service -n 100

# Последние 50 строк без пагинации
sudo journalctl -u trading-bot.service -n 50 --no-pager

# Логи за последний час
sudo journalctl -u trading-bot.service --since "1 hour ago"
```

### Поиск в логах

```bash
# Поиск ошибок
sudo journalctl -u trading-bot.service | grep -i error

# Поиск конкретного символа
sudo journalctl -u trading-bot.service | grep "SBER"

# Поиск с контекстом (5 строк до и после)
sudo journalctl -u trading-bot.service | grep -i error -A 5 -B 5
```

---

## 🚀 БЫСТРЫЕ КОМАНДЫ

### Просмотр логов в реальном времени (одна команда)

```bash
sudo journalctl -u trading-bot.service -f
```

### Просмотр последних 50 строк и затем в реальном времени

```bash
sudo journalctl -u trading-bot.service -n 50 -f
```

### Просмотр логов файла в реальном времени

```bash
tail -f /home/botuser/trading_bot/trading_bot/logs/trading_bot.log
```

---

## ⚠️ ВАЖНО

1. **Для выхода из просмотра:** Нажмите `Ctrl+C` или `q`
2. **`-f` означает "follow"** - следить за файлом в реальном времени
3. **`-n` означает количество строк** - показать последние N строк
4. **`journalctl`** - просмотр логов systemd сервиса
5. **`tail -f`** - просмотр логов файла в реальном времени

---

## 📊 МОНИТОРИНГ РАЗНЫХ ЛОГОВ

### Все логи сервиса

```bash
sudo journalctl -u trading-bot.service -f
```

### Только ошибки

```bash
sudo journalctl -u trading-bot.service -p err -f
```

### Логи с временными метками

```bash
sudo journalctl -u trading-bot.service -f --since "today"
```

### Логи файла бота

```bash
tail -f /home/botuser/trading_bot/trading_bot/logs/trading_bot.log
```

### Логи ошибок

```bash
tail -f /home/botuser/trading_bot/trading_bot/logs/bot_error.log
```

### Аудит сделок

```bash
tail -f /home/botuser/trading_bot/trading_bot/audit_logs/trades_audit.jsonl
```

---

## ✅ РЕКОМЕНДУЕМАЯ КОМАНДА

**Для постоянного мониторинга используйте:**

```bash
sudo journalctl -u trading-bot.service -f
```

Эта команда показывает все логи сервиса в реальном времени.





