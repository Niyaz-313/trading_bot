# 📦 Установка официального Tinkoff Invest SDK

## ✅ Правильный способ установки

Официальный SDK от Tinkoff устанавливается из GitHub репозитория:

```bash
pip install git+https://github.com/Tinkoff/invest-python.git
```

## 📋 Пошаговая инструкция

### Шаг 1: Убедитесь, что Git установлен

Если Git не установлен, установите его:
- Windows: https://git-scm.com/download/win
- Или используйте: `winget install Git.Git`

### Шаг 2: Установите официальный SDK

```bash
pip install git+https://github.com/Tinkoff/invest-python.git
```

**Примечание:** Пакет установится как `tinkoff-investments`, но импортируется как `tinkoff.invest`

### Шаг 3: Проверьте установку

```bash
python -c "from tinkoff.invest import Client; print('OK')"
```

Если выводит "OK" - SDK установлен правильно!

## ⚠️ Важно

- Официальный SDK НЕ доступен в PyPI под именем `invest-python` или `tinkoff-investments`
- Нужно устанавливать напрямую из GitHub
- После установки импорт: `from tinkoff.invest import Client`

## 🔧 Альтернатива (если Git недоступен)

Если Git не установлен, можно скачать репозиторий вручную и установить:

1. Скачайте ZIP с https://github.com/Tinkoff/invest-python
2. Распакуйте
3. Перейдите в папку
4. Выполните: `pip install .`

## ✅ После установки

После успешной установки:
1. Убедитесь, что `TINVEST_TOKEN` настроен в `.env`
2. Запустите: `python backtest.py`
3. Все должно работать!

