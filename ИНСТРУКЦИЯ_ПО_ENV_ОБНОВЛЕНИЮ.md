# ИНСТРУКЦИЯ ПО ОБНОВЛЕНИЮ .env ФАЙЛА

## ✅ Что уже сделано

Все изменения внесены в файлы:
- ✅ `config.py` - обновлены значения по умолчанию
- ✅ `env_template.txt` - обновлен шаблон
- ✅ `trading_strategy.py` - обновлена логика фильтров

## 📋 Нужно ли обновлять .env файл?

### Вариант 1: Если у вас НЕТ файла .env
**Ничего делать не нужно!** Бот будет использовать новые значения по умолчанию из `config.py`.

### Вариант 2: Если у вас ЕСТЬ файл .env
**Рекомендуется обновить** следующие параметры в вашем `.env` файле для применения оптимизаций:

```env
# Порог уверенности для покупок (снижен для большего количества сделок)
MIN_CONF_BUY=0.45

# Максимальное количество сделок в день (увеличено)
MAX_TRADES_PER_DAY=50

# Максимальное количество открытых позиций (увеличено)
MAX_OPEN_POSITIONS=10

# Время cooldown между сделками (уменьшено)
SYMBOL_COOLDOWN_MIN=10

# Максимальный RSI для покупок (увеличен)
RSI_MAX_BUY=75

# Минимальное значение MACD_hist/ATR для покупок (ослаблено)
MIN_MACD_HIST_ATR_RATIO_BUY=-0.4

# Количество покупок за цикл (увеличено)
MAX_BUYS_PER_CYCLE=5
```

## 🔍 Как проверить текущие значения

Если параметры не указаны в `.env`, они берутся из `config.py` с новыми значениями по умолчанию.

Чтобы проверить, какие значения используются, запустите:
```bash
python -c "from config import MIN_CONF_BUY, RSI_MAX_BUY, MAX_TRADES_PER_DAY, MAX_OPEN_POSITIONS, SYMBOL_COOLDOWN_MIN, MAX_BUYS_PER_CYCLE, MIN_MACD_HIST_ATR_RATIO_BUY; print(f'MIN_CONF_BUY={MIN_CONF_BUY}'); print(f'RSI_MAX_BUY={RSI_MAX_BUY}'); print(f'MAX_TRADES_PER_DAY={MAX_TRADES_PER_DAY}'); print(f'MAX_OPEN_POSITIONS={MAX_OPEN_POSITIONS}'); print(f'SYMBOL_COOLDOWN_MIN={SYMBOL_COOLDOWN_MIN}'); print(f'MAX_BUYS_PER_CYCLE={MAX_BUYS_PER_CYCLE}'); print(f'MIN_MACD_HIST_ATR_RATIO_BUY={MIN_MACD_HIST_ATR_RATIO_BUY}')"
```

## ⚠️ Важно

Если в вашем `.env` файле есть старые значения (например, `MIN_CONF_BUY=0.60`), они **переопределят** новые значения по умолчанию из `config.py`.

**Рекомендация**: Обновите `.env` файл или удалите эти параметры, чтобы использовать новые значения по умолчанию.

## 📝 Пример обновления .env

Если у вас есть файл `.env`, откройте его и обновите следующие строки:

**Было:**
```env
MIN_CONF_BUY=0.60
MAX_TRADES_PER_DAY=30
MAX_OPEN_POSITIONS=3
SYMBOL_COOLDOWN_MIN=30
RSI_MAX_BUY=68
MIN_MACD_HIST_ATR_RATIO_BUY=-0.15
MAX_BUYS_PER_CYCLE=2
```

**Стало:**
```env
MIN_CONF_BUY=0.45
MAX_TRADES_PER_DAY=50
MAX_OPEN_POSITIONS=10
SYMBOL_COOLDOWN_MIN=10
RSI_MAX_BUY=75
MIN_MACD_HIST_ATR_RATIO_BUY=-0.4
MAX_BUYS_PER_CYCLE=5
```

## ✅ Готово к запуску

После обновления (или если параметры не указаны в `.env`), бот готов к перезапуску с новыми оптимизированными параметрами!

