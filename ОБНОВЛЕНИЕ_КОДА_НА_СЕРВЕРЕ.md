# ОБНОВЛЕНИЕ КОДА НА СЕРВЕРЕ

## 📋 Пошаговая инструкция

### ЭТАП 1: Отправка изменений в Git (на локальной машине)

#### Вариант 1: Использовать скрипт (РЕКОМЕНДУЕТСЯ)

1. **Запустите скрипт синхронизации:**
   ```batch
   sync_to_server.bat
   ```

   Скрипт автоматически:
   - Проверит изменения
   - Добавит их в Git
   - Получит изменения с сервера (если есть)
   - Отправит ваши изменения на GitHub

#### Вариант 2: Вручную через командную строку

1. **Откройте PowerShell или CMD в папке проекта:**
   ```powershell
   cd "C:\Users\karim\OneDrive\Рабочий стол\Bot"
   ```

2. **Проверьте статус:**
   ```bash
   git status
   ```

3. **Добавьте изменения:**
   ```bash
   git add .
   ```

4. **Закоммитьте изменения:**
   ```bash
   git commit -m "Обновление отчета по дням: добавлены детали по позициям"
   ```

5. **Получите изменения с сервера (если есть):**
   ```bash
   git fetch origin
   git pull origin main --no-edit
   ```

6. **Отправьте изменения на GitHub:**
   ```bash
   git push origin main
   ```

---

### ЭТАП 2: Обновление кода на сервере

#### Вариант 1: Автоматическое обновление (РЕКОМЕНДУЕТСЯ)

Если на сервере настроен cron для автоматического обновления, просто подождите (обычно каждые 6 часов).

#### Вариант 2: Ручное обновление на сервере

1. **Подключитесь к серверу по SSH:**
   ```bash
   ssh botuser@YOUR_IPv4_ADDRESS
   ```

2. **Перейдите в директорию бота:**
   ```bash
   cd /home/botuser/trading_bot/trading_bot
   ```

3. **Если скрипта `server_update.sh` нет, получите его из Git:**
   ```bash
   git pull origin main
   chmod +x server_update.sh
   ```

4. **Запустите скрипт обновления:**
   ```bash
   ./server_update.sh
   ```

   Или вручную:
   ```bash
   # Обновить код из Git
   git fetch origin
   git pull origin main
   
   # Обновить зависимости (если нужно)
   source venv/bin/activate
   pip install -r requirements.txt
   
   # Перезапустить бота
   sudo systemctl restart trading-bot.service
   
   # Проверить статус
   sudo systemctl status trading-bot.service --no-pager
   ```

---

## 🔄 Перезапуск бота на сервере

### ✅ ДА, бота нужно перезапустить!

После обновления кода бот **автоматически перезапустится** при использовании скрипта `server_update.sh`.

### Команды для управления ботом на сервере:

```bash
# Перезапустить бота
sudo systemctl restart trading-bot.service

# Остановить бота
sudo systemctl stop trading-bot.service

# Запустить бота
sudo systemctl start trading-bot.service

# Проверить статус
sudo systemctl status trading-bot.service --no-pager

# Просмотреть логи в реальном времени
sudo journalctl -u trading-bot.service -f

# Просмотреть последние 50 строк логов
sudo journalctl -u trading-bot.service -n 50 --no-pager
```

---

## 📝 Полная последовательность действий

### На локальной машине (Windows):

1. ✅ Внесли изменения в код (например, обновили `main.py`)
2. ✅ Запустили `sync_to_server.bat` или выполнили команды Git вручную
3. ✅ Изменения отправлены на GitHub

### На сервере (Linux):

1. ✅ Подключились по SSH: `ssh botuser@YOUR_IPv4_ADDRESS`
2. ✅ Перешли в директорию: `cd /home/botuser/trading_bot/trading_bot`
3. ✅ Получили скрипт из Git (если его нет): `git pull origin main && chmod +x server_update.sh`
4. ✅ Запустили обновление: `./server_update.sh`
5. ✅ Проверили статус: `sudo systemctl status trading-bot.service`

---

## ⚠️ ВАЖНО

1. **Всегда проверяйте статус бота** после обновления
2. **Проверяйте логи** на наличие ошибок
3. **Не останавливайте бота** во время торговых часов без необходимости
4. **Делайте бэкапы** перед крупными обновлениями

---

## 🔍 Проверка успешного обновления

После обновления проверьте:

1. **Статус сервиса:**
   ```bash
   sudo systemctl status trading-bot.service
   ```
   Должно быть: `Active: active (running)`

2. **Логи на наличие ошибок:**
   ```bash
   sudo journalctl -u trading-bot.service -n 50 | grep -i error
   ```

3. **Работает ли новая функциональность:**
   - Запросите отчет через Telegram: `/day 2026-01-22`
   - Проверьте, что отображаются детали по позициям

---

## 🚨 Если что-то пошло не так

### Бот не запускается:

```bash
# Проверьте логи ошибок
sudo journalctl -u trading-bot.service -n 100 --no-pager

# Проверьте синтаксис Python
cd /home/botuser/trading_bot/trading_bot
source venv/bin/activate
python -m py_compile main.py
```

### Откатить изменения:

```bash
# На сервере
cd /home/botuser/trading_bot/trading_bot
git log --oneline -5  # Посмотреть последние коммиты
git reset --hard HEAD~1  # Откатить последний коммит
sudo systemctl restart trading-bot.service
```

---

## ✅ Быстрая команда для обновления

**На локальной машине:**
```batch
sync_to_server.bat
```

**На сервере:**
```bash
cd /home/botuser/trading_bot/trading_bot && git pull origin main && chmod +x server_update.sh && ./server_update.sh
```

Готово! 🎉
