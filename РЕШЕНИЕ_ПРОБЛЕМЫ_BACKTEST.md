# 🔧 Решение проблемы с получением исторических данных

## ❌ Проблема

Ранее использовался yfinance (Yahoo) и для российских тикеров возникали ошибки (404/No data). Сейчас в проекте это исправлено: источник исторических данных для РФ — **T‑Invest API**.

## ✅ Решения

### Решение 1: Использовать T-Invest API (РЕКОМЕНДУЕТСЯ)

T-Invest API напрямую предоставляет исторические данные по российским акциям.

**Требования:**
- TINVEST_TOKEN должен быть настроен в .env
- Бот автоматически использует T-Invest API для получения данных

**Как работает:**
1. Бот обращается к T-Invest API через метод `get_candles`
2. Получает исторические свечи за нужный период
3. Преобразует данные в формат DataFrame для анализа

**Преимущества:**
- ✅ Работает с российскими акциями
- ✅ Актуальные данные напрямую от брокера
- ✅ Поддерживает различные интервалы (1m, 5m, 1h, 1d)

### Примечание

В этой сборке бот рассчитан на РФ и использует **только T‑Invest API**. Американские тикеры и yfinance не поддерживаются.

## 🚀 Как использовать

### Вариант 1: С T-Invest API (для российских акций)

1. Убедитесь, что в `.env` указан `TINVEST_TOKEN`
2. Запустите:
   ```bash
   python backtest.py
   ```
3. Бот автоматически использует T-Invest API для получения данных

### Вариант 2: На американских акциях (для тестирования стратегии)

1. Запустите:
   ```bash
   python backtest.py
   ```
2. Введите `y` когда система предложит протестировать на американских акциях
3. (В этой сборке американские тикеры отключены — используйте только T‑Invest API.)

## 📊 Что покажет backtest

После успешного получения данных вы увидите:
- Доходность стратегии за период
- Количество сделок (покупок и продаж)
- Процент победных сделок
- Общую прибыль/убыток
- Статистику по каждой акции

## ⚠️ Важно

- **Для реальной торговли** используйте T-Invest API (данные получаются автоматически при запуске бота)
- **Для backtesting** можно использовать американские акции для проверки стратегии
- Стратегия универсальна и работает одинаково на любых акциях

## 🔍 Технические детали

### T-Invest API

Используется метод `market_data.get_candles()`:
- Получает исторические свечи по FIGI инструмента
- Поддерживает интервалы: 1m, 5m, 15m, 1h, 1d
- Возвращает данные в формате DataFrame

## ✅ Текущая реализация

Бот:
1. Использует T‑Invest API (если настроен токен и установлен SDK)
2. Кэширует исторические свечи в `data_cache/`
3. Показывает понятные сообщения об ошибках (не пытается обращаться к Yahoo)

Все исправлено и готово к использованию!
