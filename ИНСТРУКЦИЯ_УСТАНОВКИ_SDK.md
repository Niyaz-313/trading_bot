# 📦 Инструкция по установке официального Tinkoff Invest SDK

## ✅ Установка завершена!

Официальный SDK успешно установлен и работает!

## 📋 Что было установлено

1. **tinkoff-investments** (версия 0.2.0b59) - официальный SDK от Tinkoff
2. **grpcio** - для работы с gRPC
3. **protobuf** (версия 4.25.8) - для работы с protobuf сообщениями
4. **cachetools** (версия 5.5.2) - для кэширования

## ✅ Проверка установки

Выполните:
```bash
python -c "from tinkoff.invest import Client; print('OK')"
```

Если выводит "OK" - SDK установлен правильно!

## 🚀 Что дальше

1. **Убедитесь, что TINVEST_TOKEN настроен в .env**
   - Должен быть реальный токен (не `your_token_here`)
   - Получите токен: Т-Инвестиции → Настройки → Токены T-Bank Invest API

2. **Запустите backtest:**
   ```bash
   python backtest.py
   ```

3. **Бот должен получить данные за 2024 год через T-Invest API**

## 📝 Команды для установки (если нужно переустановить)

```bash
# Установка официального SDK
pip install git+https://github.com/Tinkoff/invest-python.git

# Установка зависимостей
pip install "grpcio>=1.39.0,<2.0.0"
pip install "protobuf>=4.21.6,<5.0.0"
pip install "cachetools>=5.2.0,<6.0.0"
```

## ⚠️ Важно

- Официальный SDK устанавливается из GitHub (не из PyPI)
- Пакет называется `tinkoff-investments`, но импортируется как `tinkoff.invest`
- Требуется Git для установки из GitHub

## ✅ Все готово!

SDK установлен и готов к работе. Запустите `python backtest.py` для тестирования!






