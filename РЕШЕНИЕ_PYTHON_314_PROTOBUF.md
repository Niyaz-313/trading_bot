# ✅ Решение проблемы protobuf с Python 3.14

## ❌ Проблема

При использовании Python 3.14 возникает ошибка:
```
TypeError: Metaclasses with custom tp_new are not supported.
```

Это связано с несовместимостью старых версий protobuf (<5.0.0) с Python 3.14.

## ✅ Решение

Установите protobuf версии 6.x, которая поддерживает Python 3.14:

```bash
pip install --upgrade protobuf
```

После этого будет установлена protobuf 6.33.2, которая совместима с Python 3.14.

## ⚠️ Важно

- SDK требует protobuf<5.0.0, но на практике работает с версией 6.x
- Предупреждение pip о несовместимости можно игнорировать
- Все функции SDK работают корректно

## ✅ Проверка

После обновления проверьте:

```bash
python -c "from tinkoff.invest import Client; print('OK')"
```

Если выводит "OK" - все работает!

## 📋 Итоговая установка для Python 3.14

```bash
# Установка официального SDK
pip install git+https://github.com/Tinkoff/invest-python.git

# Установка зависимостей
pip install grpcio
pip install --upgrade protobuf  # Важно! Версия 6.x для Python 3.14
pip install cachetools
```

Все готово к работе! 🚀







