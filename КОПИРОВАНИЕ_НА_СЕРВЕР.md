# КОПИРОВАНИЕ ФАЙЛОВ НА СЕРВЕР (Windows PowerShell)

## ❌ Проблема
В PowerShell команда `cp` - это алиас для `Copy-Item`, который работает только с локальными путями.
Для копирования на удаленный сервер нужно использовать `scp`.

---

## ✅ РЕШЕНИЕ 1: Использовать `scp` (рекомендуется)

### Если OpenSSH установлен:
```powershell
scp main.py botuser@[2a03:6f00:a::1:a8f1]:~/trading_bot/
```

**ВАЖНО:** IPv6 адрес должен быть в квадратных скобках `[ ]`

### Если `scp` не найден, используйте полный путь:
```powershell
C:\Windows\System32\OpenSSH\scp.exe main.py botuser@[2a03:6f00:a::1:a8f1]:~/trading_bot/
```

---

## ✅ РЕШЕНИЕ 2: Установить OpenSSH (если не установлен)

### Проверка установки:
```powershell
Get-WindowsCapability -Online | Where-Object Name -like 'OpenSSH*'
```

### Установка OpenSSH клиента:
```powershell
# Запустите PowerShell от имени администратора
Add-WindowsCapability -Online -Name OpenSSH.Client~~~~0.0.1.0
```

После установки перезапустите PowerShell и используйте команду из Решения 1.

---

## ✅ РЕШЕНИЕ 3: Использовать Git Bash (если установлен Git)

Если у вас установлен Git, можно использовать Git Bash:

1. Откройте Git Bash
2. Выполните:
```bash
scp main.py botuser@[2a03:6f00:a::1:a8f1]:~/trading_bot/
```

---

## ✅ РЕШЕНИЕ 4: Использовать WinSCP (графический интерфейс)

1. Скачайте и установите WinSCP: https://winscp.net/
2. Подключитесь к серверу:
   - Хост: `2a03:6f00:a::1:a8f1`
   - Пользователь: `botuser`
   - Протокол: SFTP
3. Перетащите файл `main.py` в папку `~/trading_bot/`

---

## ✅ РЕШЕНИЕ 5: Использовать PowerShell модуль Posh-SSH

### Установка модуля:
```powershell
Install-Module -Name Posh-SSH -Scope CurrentUser
```

### Использование:
```powershell
# Импорт модуля
Import-Module Posh-SSH

# Создание сессии
$session = New-SSHSession -ComputerName 2a03:6f00:a::1:a8f1 -Credential (Get-Credential botuser)

# Копирование файла
Set-SCPFile -ComputerName 2a03:6f00:a::1:a8f1 -Credential (Get-Credential botuser) -LocalFile main.py -RemotePath ~/trading_bot/
```

---

## 🔧 ПРОВЕРКА УСТАНОВКИ OpenSSH

### Проверить, установлен ли OpenSSH:
```powershell
Get-Command ssh -ErrorAction SilentlyContinue
Get-Command scp -ErrorAction SilentlyContinue
```

### Если команды не найдены, установите OpenSSH (см. Решение 2)

---

## 📝 ПРИМЕРЫ КОМАНД

### Копирование одного файла:
```powershell
scp main.py botuser@[2a03:6f00:a::1:a8f1]:~/trading_bot/
```

### Копирование нескольких файлов:
```powershell
scp main.py config.py botuser@[2a03:6f00:a::1:a8f1]:~/trading_bot/
```

### Копирование директории (рекурсивно):
```powershell
scp -r logs botuser@[2a03:6f00:a::1:a8f1]:~/trading_bot/
```

### Копирование с указанием порта (если не стандартный 22):
```powershell
scp -P 22 main.py botuser@[2a03:6f00:a::1:a8f1]:~/trading_bot/
```

---

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

1. **IPv6 адрес всегда в квадратных скобках:** `[2a03:6f00:a::1:a8f1]`
2. **SSH ключи:** Если настроены SSH ключи, они будут использоваться автоматически
3. **Пароль:** Если SSH ключи не настроены, будет запрошен пароль
4. **Путь:** Используйте `~/` для домашней директории пользователя

---

## 🚀 БЫСТРОЕ РЕШЕНИЕ

Если OpenSSH уже установлен, просто выполните:

```powershell
scp main.py botuser@[2a03:6f00:a::1:a8f1]:~/trading_bot/
```

Если не работает, попробуйте полный путь:

```powershell
C:\Windows\System32\OpenSSH\scp.exe main.py botuser@[2a03:6f00:a::1:a8f1]:~/trading_bot/
```

