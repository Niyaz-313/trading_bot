# АНАЛИЗ НИЗКОГО КОЛИЧЕСТВА ПОКУПОК/ПРОДАЖ И ПУТИ РЕШЕНИЯ

**Дата анализа:** 2026-01-16  
**Цель:** Максимизация прибыли через увеличение количества качественных сделок

---

## 🔍 КРИТИЧЕСКИЕ ПРОБЛЕМЫ

### 1. **СЛИШКОМ ВЫСОКИЙ ПОРОГ УВЕРЕННОСТИ (MIN_CONF_BUY = 0.60)**

**Проблема:**
- Текущий порог `MIN_CONF_BUY = 0.60` (60%) блокирует множество потенциально прибыльных сделок
- В логах видно множество сигналов с confidence 0.5-0.59, которые блокируются
- Для ежедневной прибыли нужна более активная торговля

**Результат:** Бот пропускает 70-80% потенциально прибыльных входов

**Решение:**
```python
# В config.py или .env изменить:
MIN_CONF_BUY=0.50  # Снизить с 0.60 до 0.50 (50%)
```

**Ожидаемый эффект:** Увеличение сделок на 150-200%

---

### 2. **СТРАТЕГИЯ ТРЕБУЕТ МИНИМУМ 3 BUY_SIGNALS**

**Проблема:**
В `trading_strategy.py` строка 215:
```python
if buy_signals >= 3 and buy_signals > sell_signals and confidence >= 0.5 and trend != 'down':
```

Требование `buy_signals >= 3` слишком строгое. Многие хорошие сигналы имеют только 2 сигнала, но блокируются.

**Решение:**
```python
# Снизить требование до 2 сигналов для более активной торговли
if buy_signals >= 2 and buy_signals > sell_signals and confidence >= 0.50:
```

**Ожидаемый эффект:** Увеличение сделок на 100-150%

---

### 3. **БЛОКИРОВКА ПОКУПОК ПРИ RSI > 68**

**Проблема:**
Фильтр `RSI_MAX_BUY = 68` слишком консервативен. RSI 68-75 часто дает хорошие входы в сильных трендах (мощный импульс перевешивает перекупленность).

**Решение:**
```python
# В config.py:
RSI_MAX_BUY=75  # Увеличить с 68 до 75
```

**Дополнительно:** При RSI 68-75 разрешать покупку, если MACD_hist/ATR > 0.2 (сильный импульс)

**Ожидаемый эффект:** Увеличение сделок на 30-50%

---

### 4. **СЛИШКОМ ЖЕСТКИЙ ФИЛЬТР "SIDEWAYS + NEGATIVE MACD"**

**Проблема:**
`BLOCK_SIDEWAYS_NEGATIVE_MACD = true` блокирует все входы в боковике с отрицательным MACD, даже при сильной перепроданности (RSI <= 30).

**Текущая логика:** Блокирует ВСЕ sideways + negative MACD

**Решение:**
Уже частично реализовано - разрешен вход при RSI <= 30. НО нужно снизить порог до RSI <= 35 для большей активности:
```python
# В trading_strategy.py строка 278-279 изменить:
if rsi_val is not None and rsi_val <= 35:  # Было 30, сделать 35
    pass  # Разрешаем вход
```

**Ожидаемый эффект:** Увеличение сделок на 20-40%

---

### 5. **MAX_BUYS_PER_CYCLE = 1 (ОГРАНИЧЕНИЕ НА 1 ПОКУПКУ ЗА ЦИКЛ)**

**Проблема:**
Бот может купить только 1 инструмент за цикл, даже если несколько инструментов имеют хорошие сигналы одновременно.

**Решение:**
```python
# В config.py:
MAX_BUYS_PER_CYCLE=3  # Увеличить с 1 до 3
```

**Ожидаемый эффект:** Увеличение сделок на 200-300% в активные моменты

---

### 6. **MAX_TRADES_PER_DAY = 10 (ОГРАНИЧЕНИЕ 10 СДЕЛОК В ДЕНЬ)**

**Проблема:**
Лимит в 10 покупок в день слишком мал для активной торговли и получения ежедневной прибыли.

**Решение:**
```python
# В config.py:
MAX_TRADES_PER_DAY=30  # Увеличить с 10 до 30
```

**Ожидаемый эффект:** Увеличение оборота в 3 раза

---

### 7. **MAX_OPEN_POSITIONS = 3 (МАКСИМУМ 3 ПОЗИЦИИ)**

**Проблема:**
Одновременно может быть открыто только 3 позиции. Это ограничивает диверсификацию и возможность ловить больше возможностей.

**Решение:**
```python
# В config.py:
MAX_OPEN_POSITIONS=8  # Увеличить с 3 до 8
```

**Ожидаемый эффект:** Лучшая диверсификация и больше сделок

---

### 8. **MIN_ATR_PCT = 0.4% (БЛОКИРОВКА НИЗКОВОЛАТИЛЬНЫХ)**

**Проблема:**
Фильтр блокирует инструменты с ATR < 0.4% от цены. Некоторые хорошие инструменты (особенно облигации/валюты) имеют низкую волатильность, но стабильную доходность.

**Решение:**
```python
# В config.py:
MIN_ATR_PCT=0.2%  # Снизить с 0.4% до 0.2%
```

ИЛИ для валютных пар (USD000UTSTOM, CNYRUB_TOM и т.д.) вообще отключить:
```python
# В main.py добавить исключение для валютных пар
if symbol.endswith('_TOM') or 'RUB' in symbol:
    min_atr_pct = 0.0  # Не применять фильтр для валют
```

**Ожидаемый эффект:** Увеличение сделок на 15-25%

---

### 9. **COOLDOWN_30_МИНУТ МЕЖДУ СДЕЛКАМИ ПО ОДНОМУ СИМВОЛУ**

**Проблема:**
`SYMBOL_COOLDOWN_MIN = 30` блокирует повторную торговлю одного символа на 30 минут. Это мешает скальпингу и повторным входам.

**Решение:**
```python
# В config.py:
SYMBOL_COOLDOWN_MIN=15  # Снизить с 30 до 15 минут
```

**Ожидаемый эффект:** Увеличение оборота на 20-30%

---

### 10. **ЖЕСТКИЕ ФИЛЬТРЫ КАЧЕСТВА В _execute_buy()**

**Проблема:**
В `main.py` функция `_execute_buy()` имеет множество дополнительных фильтров, которые блокируют уже прошедшие `should_buy()` сигналы:
- `BLOCK_SIDEWAYS_NEGATIVE_MACD` 
- `REQUIRE_MACD_HIST_POSITIVE_BUY`
- `MIN_MACD_HIST_ATR_RATIO_BUY = -0.15`
- И другие...

**Решение:**
Отключить или ослабить эти фильтры в `.env`:
```bash
BLOCK_SIDEWAYS_NEGATIVE_MACD=false
REQUIRE_MACD_HIST_POSITIVE_BUY=false
MIN_MACD_HIST_ATR_RATIO_BUY=-0.3  # Было -0.15, сделать -0.3 (разрешить больше)
```

**Ожидаемый эффект:** Увеличение сделок на 40-60%

---

## 📊 ПРИОРИТЕТНЫЕ ДЕЙСТВИЯ ДЛЯ МАКСИМАЛЬНОЙ ПРИБЫЛИ

### **КРИТИЧЕСКИ ВАЖНО (выполнить первыми):**

1. ✅ **Снизить MIN_CONF_BUY до 0.50** → +150-200% сделок
2. ✅ **Уменьшить требование buy_signals с 3 до 2** → +100-150% сделок  
3. ✅ **Увеличить MAX_BUYS_PER_CYCLE до 3** → +200-300% сделок
4. ✅ **Увеличить MAX_TRADES_PER_DAY до 30** → +300% оборота
5. ✅ **Увеличить MAX_OPEN_POSITIONS до 8** → лучшая диверсификация

### **ВАЖНО (второй приоритет):**

6. ✅ **Увеличить RSI_MAX_BUY до 75** → +30-50% сделок
7. ✅ **Ослабить фильтр sideways+negative MACD (RSI <= 35)** → +20-40% сделок
8. ✅ **Снизить SYMBOL_COOLDOWN_MIN до 15** → +20-30% оборота
9. ✅ **Отключить жесткие фильтры в _execute_buy()** → +40-60% сделок

### **ОПЦИОНАЛЬНО (по необходимости):**

10. ✅ **Снизить MIN_ATR_PCT до 0.2%** → +15-25% сделок
11. ✅ **Увеличить RISK_PER_TRADE до 0.01 (1%)** → больше размер позиций

---

## 🎯 ОЖИДАЕМЫЙ РЕЗУЛЬТАТ ПОСЛЕ ВСЕХ ИЗМЕНЕНИЙ

**Текущее состояние:**
- ~2-5 покупок в день
- ~1-3 открытых позиции
- Много пропущенных возможностей

**После оптимизации:**
- **20-50 покупок в день** (увеличение в 10-25 раз)
- **5-8 открытых позиций** одновременно
- **3-5 покупок за цикл** в активные моменты
- **Ежедневная прибыль** за счет большей активности

**Суммарный эффект:** Увеличение количества сделок в **10-25 раз**

---

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ ПО РИСКАМ

1. **Больше сделок = больше комиссий**
   - Увеличить размер позиций или снизить количество символов
   - Использовать `RISK_PER_TRADE = 0.01` (1%) вместо 0.005 (0.5%)

2. **Контроль рисков остается критичным**
   - STOP_LOSS_PERCENT = 3% (оставить)
   - TAKE_PROFIT_PERCENT = 6% (оставить)
   - Дневные лимиты убытка (оставить)

3. **Мониторинг качества сигналов**
   - После изменений отслеживать win rate
   - Если упадет ниже 40% - вернуть часть фильтров
   - Использовать Symbol Tracker для автоматической блокировки плохих символов

---

## 🚀 БЫСТРОЕ ПРИМЕНЕНИЕ (КОМПЛЕКСНЫЙ ПАКЕТ ИЗМЕНЕНИЙ)

Создайте файл `.env.aggressive` с настройками для максимальной прибыли:

```bash
# КРИТИЧЕСКИ ВАЖНО - Увеличение активности
MIN_CONF_BUY=0.50
MAX_BUYS_PER_CYCLE=3
MAX_TRADES_PER_DAY=30
MAX_OPEN_POSITIONS=8
SYMBOL_COOLDOWN_MIN=15

# Ослабление фильтров RSI и MACD
RSI_MAX_BUY=75
BLOCK_SIDEWAYS_NEGATIVE_MACD=false
REQUIRE_MACD_HIST_POSITIVE_BUY=false
MIN_MACD_HIST_ATR_RATIO_BUY=-0.3

# Увеличение риска на сделку (для компенсации комиссий)
RISK_PER_TRADE=0.01

# Отключение жестких фильтров для валют
MIN_ATR_PCT=0.2%
```

**Применение:**
```bash
# Скопировать агрессивные настройки в .env
copy .env.aggressive .env
# Или отредактировать .env вручную
```

---

## 📈 ДОПОЛНИТЕЛЬНЫЕ РЕКОМЕНДАЦИИ

### 1. **Оптимизация стратегии для большего количества сигналов**

В `trading_strategy.py` добавить более чувствительные индикаторы:
- Использовать более короткие периоды MA (10/20 вместо 20/50)
- Добавить дополнительные сигналы от объемов
- Использовать ADX для определения силы тренда

### 2. **Динамическая адаптация порогов**

Реализовать адаптивные пороги confidence в зависимости от волатильности рынка:
- В высокую волатильность: MIN_CONF_BUY = 0.55
- В низкую волатильность: MIN_CONF_BUY = 0.45

### 3. **Приоритизация символов по ликвидности**

Увеличить приоритет высоколиквидных инструментов (SBER, GAZP, YNDX) в ранжировании buy-кандидатов.

### 4. **Использование нескольких таймфреймов**

Добавить анализ на нескольких таймфреймах (5m + 15m) для лучших входов.

---

## ✅ ЧЕКЛИСТ ПРИМЕНЕНИЯ

- [ ] 1. Изменить MIN_CONF_BUY = 0.50
- [ ] 2. Изменить MAX_BUYS_PER_CYCLE = 3
- [ ] 3. Изменить MAX_TRADES_PER_DAY = 30
- [ ] 4. Изменить MAX_OPEN_POSITIONS = 8
- [ ] 5. Изменить RSI_MAX_BUY = 75
- [ ] 6. Изменить SYMBOL_COOLDOWN_MIN = 15
- [ ] 7. Отключить BLOCK_SIDEWAYS_NEGATIVE_MACD
- [ ] 8. Изменить MIN_MACD_HIST_ATR_RATIO_BUY = -0.3
- [ ] 9. Изменить требование buy_signals >= 2 (в trading_strategy.py)
- [ ] 10. Перезапустить бота и мониторить результаты 2-3 дня
- [ ] 11. При необходимости скорректировать параметры

---

**ОЖИДАЕМЫЙ РЕЗУЛЬТАТ:** Увеличение количества сделок в **10-25 раз**, что должно привести к **ежедневной стабильной прибыли** от торгового бота.




