# 📦 РАЗВЕРТЫВАНИЕ БОТА НА УДАЛЕННОМ СЕРВЕРЕ

## 🎯 Цель
Запустить торгового бота на удаленном сервере 24/7 с синхронизацией кода с локальной машины.

---

## 📚 ДОКУМЕНТАЦИЯ

### Основные инструкции:
1. **РАЗВЕРТЫВАНИЕ_НА_СЕРВЕРЕ.md** - Полная подробная инструкция (11 этапов)
2. **БЫСТРЫЙ_СТАРТ_СЕРВЕР.md** - Краткая инструкция для быстрого старта
3. **ИТОГОВАЯ_ИНСТРУКЦИЯ_СЕРВЕР.md** - Итоговая сводка со всеми шагами

### Скрипты для локальной машины (Windows):
- **sync_to_server.bat** - Синхронизация кода с сервером через Git
- **backup_from_server.bat** - Резервное копирование данных с сервера

### Скрипты для сервера (Linux):
- **server_setup.sh** - Первоначальная настройка сервера
- **server_update.sh** - Обновление бота на сервере
- **server_backup.sh** - Резервное копирование на сервере
- **create_systemd_service.sh** - Создание systemd сервиса

---

## 🚀 БЫСТРЫЙ СТАРТ

### 1. На локальной машине:

```bash
# Инициализируйте Git
cd "C:\Users\karim\OneDrive\Рабочий стол\Bot"
git init
git add .
git commit -m "Initial commit"
git branch -M main

# Создайте репозиторий на GitHub и добавьте remote
git remote add origin https://github.com/ваш_username/trading_bot.git
git push -u origin main
```

### 2. На сервере:

```bash
# Подключитесь к серверу
ssh botuser@ваш_сервер_ip

# Запустите скрипт настройки
chmod +x ~/server_setup.sh
~/server_setup.sh

# Клонируйте репозиторий
cd ~/trading_bot
git clone https://github.com/ваш_username/trading_bot.git .

# Настройте окружение
python3.11 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# Скопируйте .env (с локальной машины)
# scp .env botuser@сервер:~/trading_bot/.env

# Создайте systemd сервис
chmod +x ~/trading_bot/create_systemd_service.sh
~/trading_bot/create_systemd_service.sh

# Запустите бота
sudo systemctl start trading-bot.service
```

---

## 📋 ЧЕКЛИСТ

- [ ] Выбран VPS сервер
- [ ] Настроен пользователь botuser
- [ ] Настроены SSH ключи
- [ ] Установлен Python и зависимости
- [ ] Код синхронизирован через Git
- [ ] Создан .env файл на сервере
- [ ] Бот запущен как systemd сервис
- [ ] Настроена автоматическая синхронизация
- [ ] Настроено резервное копирование
- [ ] Проверена работоспособность

---

## 🔄 ЕЖЕДНЕВНАЯ РАБОТА

### Обновление кода:
1. Внесите изменения на локальной машине
2. Запустите `sync_to_server.bat`
3. Сервер обновится автоматически (каждые 6 часов) или вручную

### Резервное копирование:
- Автоматически: каждый день в 3:00 на сервере
- Вручную: запустите `backup_from_server.bat`

---

## 🛠️ УПРАВЛЕНИЕ

```bash
# Статус
sudo systemctl status trading-bot.service

# Перезапуск
sudo systemctl restart trading-bot.service

# Логи
sudo journalctl -u trading-bot.service -f
```

---

**Подробные инструкции см. в РАЗВЕРТЫВАНИЕ_НА_СЕРВЕРЕ.md**


## 🎯 Цель
Запустить торгового бота на удаленном сервере 24/7 с синхронизацией кода с локальной машины.

---

## 📚 ДОКУМЕНТАЦИЯ

### Основные инструкции:
1. **РАЗВЕРТЫВАНИЕ_НА_СЕРВЕРЕ.md** - Полная подробная инструкция (11 этапов)
2. **БЫСТРЫЙ_СТАРТ_СЕРВЕР.md** - Краткая инструкция для быстрого старта
3. **ИТОГОВАЯ_ИНСТРУКЦИЯ_СЕРВЕР.md** - Итоговая сводка со всеми шагами

### Скрипты для локальной машины (Windows):
- **sync_to_server.bat** - Синхронизация кода с сервером через Git
- **backup_from_server.bat** - Резервное копирование данных с сервера

### Скрипты для сервера (Linux):
- **server_setup.sh** - Первоначальная настройка сервера
- **server_update.sh** - Обновление бота на сервере
- **server_backup.sh** - Резервное копирование на сервере
- **create_systemd_service.sh** - Создание systemd сервиса

---

## 🚀 БЫСТРЫЙ СТАРТ

### 1. На локальной машине:

```bash
# Инициализируйте Git
cd "C:\Users\karim\OneDrive\Рабочий стол\Bot"
git init
git add .
git commit -m "Initial commit"
git branch -M main

# Создайте репозиторий на GitHub и добавьте remote
git remote add origin https://github.com/ваш_username/trading_bot.git
git push -u origin main
```

### 2. На сервере:

```bash
# Подключитесь к серверу
ssh botuser@ваш_сервер_ip

# Запустите скрипт настройки
chmod +x ~/server_setup.sh
~/server_setup.sh

# Клонируйте репозиторий
cd ~/trading_bot
git clone https://github.com/ваш_username/trading_bot.git .

# Настройте окружение
python3.11 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# Скопируйте .env (с локальной машины)
# scp .env botuser@сервер:~/trading_bot/.env

# Создайте systemd сервис
chmod +x ~/trading_bot/create_systemd_service.sh
~/trading_bot/create_systemd_service.sh

# Запустите бота
sudo systemctl start trading-bot.service
```

---

## 📋 ЧЕКЛИСТ

- [ ] Выбран VPS сервер
- [ ] Настроен пользователь botuser
- [ ] Настроены SSH ключи
- [ ] Установлен Python и зависимости
- [ ] Код синхронизирован через Git
- [ ] Создан .env файл на сервере
- [ ] Бот запущен как systemd сервис
- [ ] Настроена автоматическая синхронизация
- [ ] Настроено резервное копирование
- [ ] Проверена работоспособность

---

## 🔄 ЕЖЕДНЕВНАЯ РАБОТА

### Обновление кода:
1. Внесите изменения на локальной машине
2. Запустите `sync_to_server.bat`
3. Сервер обновится автоматически (каждые 6 часов) или вручную

### Резервное копирование:
- Автоматически: каждый день в 3:00 на сервере
- Вручную: запустите `backup_from_server.bat`

---

## 🛠️ УПРАВЛЕНИЕ

```bash
# Статус
sudo systemctl status trading-bot.service

# Перезапуск
sudo systemctl restart trading-bot.service

# Логи
sudo journalctl -u trading-bot.service -f
```

---

**Подробные инструкции см. в РАЗВЕРТЫВАНИЕ_НА_СЕРВЕРЕ.md**


## 🎯 Цель
Запустить торгового бота на удаленном сервере 24/7 с синхронизацией кода с локальной машины.

---

## 📚 ДОКУМЕНТАЦИЯ

### Основные инструкции:
1. **РАЗВЕРТЫВАНИЕ_НА_СЕРВЕРЕ.md** - Полная подробная инструкция (11 этапов)
2. **БЫСТРЫЙ_СТАРТ_СЕРВЕР.md** - Краткая инструкция для быстрого старта
3. **ИТОГОВАЯ_ИНСТРУКЦИЯ_СЕРВЕР.md** - Итоговая сводка со всеми шагами

### Скрипты для локальной машины (Windows):
- **sync_to_server.bat** - Синхронизация кода с сервером через Git
- **backup_from_server.bat** - Резервное копирование данных с сервера

### Скрипты для сервера (Linux):
- **server_setup.sh** - Первоначальная настройка сервера
- **server_update.sh** - Обновление бота на сервере
- **server_backup.sh** - Резервное копирование на сервере
- **create_systemd_service.sh** - Создание systemd сервиса

---

## 🚀 БЫСТРЫЙ СТАРТ

### 1. На локальной машине:

```bash
# Инициализируйте Git
cd "C:\Users\karim\OneDrive\Рабочий стол\Bot"
git init
git add .
git commit -m "Initial commit"
git branch -M main

# Создайте репозиторий на GitHub и добавьте remote
git remote add origin https://github.com/ваш_username/trading_bot.git
git push -u origin main
```

### 2. На сервере:

```bash
# Подключитесь к серверу
ssh botuser@ваш_сервер_ip

# Запустите скрипт настройки
chmod +x ~/server_setup.sh
~/server_setup.sh

# Клонируйте репозиторий
cd ~/trading_bot
git clone https://github.com/ваш_username/trading_bot.git .

# Настройте окружение
python3.11 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# Скопируйте .env (с локальной машины)
# scp .env botuser@сервер:~/trading_bot/.env

# Создайте systemd сервис
chmod +x ~/trading_bot/create_systemd_service.sh
~/trading_bot/create_systemd_service.sh

# Запустите бота
sudo systemctl start trading-bot.service
```

---

## 📋 ЧЕКЛИСТ

- [ ] Выбран VPS сервер
- [ ] Настроен пользователь botuser
- [ ] Настроены SSH ключи
- [ ] Установлен Python и зависимости
- [ ] Код синхронизирован через Git
- [ ] Создан .env файл на сервере
- [ ] Бот запущен как systemd сервис
- [ ] Настроена автоматическая синхронизация
- [ ] Настроено резервное копирование
- [ ] Проверена работоспособность

---

## 🔄 ЕЖЕДНЕВНАЯ РАБОТА

### Обновление кода:
1. Внесите изменения на локальной машине
2. Запустите `sync_to_server.bat`
3. Сервер обновится автоматически (каждые 6 часов) или вручную

### Резервное копирование:
- Автоматически: каждый день в 3:00 на сервере
- Вручную: запустите `backup_from_server.bat`

---

## 🛠️ УПРАВЛЕНИЕ

```bash
# Статус
sudo systemctl status trading-bot.service

# Перезапуск
sudo systemctl restart trading-bot.service

# Логи
sudo journalctl -u trading-bot.service -f
```

---

**Подробные инструкции см. в РАЗВЕРТЫВАНИЕ_НА_СЕРВЕРЕ.md**


## 🎯 Цель
Запустить торгового бота на удаленном сервере 24/7 с синхронизацией кода с локальной машины.

---

## 📚 ДОКУМЕНТАЦИЯ

### Основные инструкции:
1. **РАЗВЕРТЫВАНИЕ_НА_СЕРВЕРЕ.md** - Полная подробная инструкция (11 этапов)
2. **БЫСТРЫЙ_СТАРТ_СЕРВЕР.md** - Краткая инструкция для быстрого старта
3. **ИТОГОВАЯ_ИНСТРУКЦИЯ_СЕРВЕР.md** - Итоговая сводка со всеми шагами

### Скрипты для локальной машины (Windows):
- **sync_to_server.bat** - Синхронизация кода с сервером через Git
- **backup_from_server.bat** - Резервное копирование данных с сервера

### Скрипты для сервера (Linux):
- **server_setup.sh** - Первоначальная настройка сервера
- **server_update.sh** - Обновление бота на сервере
- **server_backup.sh** - Резервное копирование на сервере
- **create_systemd_service.sh** - Создание systemd сервиса

---

## 🚀 БЫСТРЫЙ СТАРТ

### 1. На локальной машине:

```bash
# Инициализируйте Git
cd "C:\Users\karim\OneDrive\Рабочий стол\Bot"
git init
git add .
git commit -m "Initial commit"
git branch -M main

# Создайте репозиторий на GitHub и добавьте remote
git remote add origin https://github.com/ваш_username/trading_bot.git
git push -u origin main
```

### 2. На сервере:

```bash
# Подключитесь к серверу
ssh botuser@ваш_сервер_ip

# Запустите скрипт настройки
chmod +x ~/server_setup.sh
~/server_setup.sh

# Клонируйте репозиторий
cd ~/trading_bot
git clone https://github.com/ваш_username/trading_bot.git .

# Настройте окружение
python3.11 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# Скопируйте .env (с локальной машины)
# scp .env botuser@сервер:~/trading_bot/.env

# Создайте systemd сервис
chmod +x ~/trading_bot/create_systemd_service.sh
~/trading_bot/create_systemd_service.sh

# Запустите бота
sudo systemctl start trading-bot.service
```

---

## 📋 ЧЕКЛИСТ

- [ ] Выбран VPS сервер
- [ ] Настроен пользователь botuser
- [ ] Настроены SSH ключи
- [ ] Установлен Python и зависимости
- [ ] Код синхронизирован через Git
- [ ] Создан .env файл на сервере
- [ ] Бот запущен как systemd сервис
- [ ] Настроена автоматическая синхронизация
- [ ] Настроено резервное копирование
- [ ] Проверена работоспособность

---

## 🔄 ЕЖЕДНЕВНАЯ РАБОТА

### Обновление кода:
1. Внесите изменения на локальной машине
2. Запустите `sync_to_server.bat`
3. Сервер обновится автоматически (каждые 6 часов) или вручную

### Резервное копирование:
- Автоматически: каждый день в 3:00 на сервере
- Вручную: запустите `backup_from_server.bat`

---

## 🛠️ УПРАВЛЕНИЕ

```bash
# Статус
sudo systemctl status trading-bot.service

# Перезапуск
sudo systemctl restart trading-bot.service

# Логи
sudo journalctl -u trading-bot.service -f
```

---

**Подробные инструкции см. в РАЗВЕРТЫВАНИЕ_НА_СЕРВЕРЕ.md**


## 🎯 Цель
Запустить торгового бота на удаленном сервере 24/7 с синхронизацией кода с локальной машины.

---

## 📚 ДОКУМЕНТАЦИЯ

### Основные инструкции:
1. **РАЗВЕРТЫВАНИЕ_НА_СЕРВЕРЕ.md** - Полная подробная инструкция (11 этапов)
2. **БЫСТРЫЙ_СТАРТ_СЕРВЕР.md** - Краткая инструкция для быстрого старта
3. **ИТОГОВАЯ_ИНСТРУКЦИЯ_СЕРВЕР.md** - Итоговая сводка со всеми шагами

### Скрипты для локальной машины (Windows):
- **sync_to_server.bat** - Синхронизация кода с сервером через Git
- **backup_from_server.bat** - Резервное копирование данных с сервера

### Скрипты для сервера (Linux):
- **server_setup.sh** - Первоначальная настройка сервера
- **server_update.sh** - Обновление бота на сервере
- **server_backup.sh** - Резервное копирование на сервере
- **create_systemd_service.sh** - Создание systemd сервиса

---

## 🚀 БЫСТРЫЙ СТАРТ

### 1. На локальной машине:

```bash
# Инициализируйте Git
cd "C:\Users\karim\OneDrive\Рабочий стол\Bot"
git init
git add .
git commit -m "Initial commit"
git branch -M main

# Создайте репозиторий на GitHub и добавьте remote
git remote add origin https://github.com/ваш_username/trading_bot.git
git push -u origin main
```

### 2. На сервере:

```bash
# Подключитесь к серверу
ssh botuser@ваш_сервер_ip

# Запустите скрипт настройки
chmod +x ~/server_setup.sh
~/server_setup.sh

# Клонируйте репозиторий
cd ~/trading_bot
git clone https://github.com/ваш_username/trading_bot.git .

# Настройте окружение
python3.11 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# Скопируйте .env (с локальной машины)
# scp .env botuser@сервер:~/trading_bot/.env

# Создайте systemd сервис
chmod +x ~/trading_bot/create_systemd_service.sh
~/trading_bot/create_systemd_service.sh

# Запустите бота
sudo systemctl start trading-bot.service
```

---

## 📋 ЧЕКЛИСТ

- [ ] Выбран VPS сервер
- [ ] Настроен пользователь botuser
- [ ] Настроены SSH ключи
- [ ] Установлен Python и зависимости
- [ ] Код синхронизирован через Git
- [ ] Создан .env файл на сервере
- [ ] Бот запущен как systemd сервис
- [ ] Настроена автоматическая синхронизация
- [ ] Настроено резервное копирование
- [ ] Проверена работоспособность

---

## 🔄 ЕЖЕДНЕВНАЯ РАБОТА

### Обновление кода:
1. Внесите изменения на локальной машине
2. Запустите `sync_to_server.bat`
3. Сервер обновится автоматически (каждые 6 часов) или вручную

### Резервное копирование:
- Автоматически: каждый день в 3:00 на сервере
- Вручную: запустите `backup_from_server.bat`

---

## 🛠️ УПРАВЛЕНИЕ

```bash
# Статус
sudo systemctl status trading-bot.service

# Перезапуск
sudo systemctl restart trading-bot.service

# Логи
sudo journalctl -u trading-bot.service -f
```

---

**Подробные инструкции см. в РАЗВЕРТЫВАНИЕ_НА_СЕРВЕРЕ.md**





