# ИТОГОВАЯ ИНСТРУКЦИЯ: РАЗВЕРТЫВАНИЕ БОТА НА СЕРВЕРЕ

## 📋 Что было создано

### Документация:
1. **РАЗВЕРТЫВАНИЕ_НА_СЕРВЕРЕ.md** - Полная подробная инструкция (11 этапов)
2. **БЫСТРЫЙ_СТАРТ_СЕРВЕР.md** - Краткая инструкция для быстрого старта

### Скрипты для локальной машины (Windows):
1. **sync_to_server.bat** - Синхронизация кода с сервером через Git
2. **backup_from_server.bat** - Резервное копирование данных с сервера

### Скрипты для сервера (Linux):
1. **server_setup.sh** - Первоначальная настройка сервера
2. **server_update.sh** - Обновление бота на сервере
3. **server_backup.sh** - Резервное копирование на сервере
4. **create_systemd_service.sh** - Создание systemd сервиса

### Конфигурация:
1. **.gitignore** - Исключение конфиденциальных файлов из Git

---

## 🚀 БЫСТРЫЙ СТАРТ (пошагово)

### ШАГ 1: Подготовка на локальной машине

1. **Инициализируйте Git репозиторий:**
   ```bash
   cd "C:\Users\karim\OneDrive\Рабочий стол\Bot"
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   ```

2. **Создайте репозиторий на GitHub:**
   - Зайдите на https://github.com
   - Создайте новый приватный репозиторий (например, `trading_bot`)
   - Добавьте remote:
   ```bash
   git remote add origin https://github.com/ваш_username/trading_bot.git
   git push -u origin main
   ```

3. **Проверьте .gitignore:**
   - Убедитесь, что `.env`, `*.log`, `audit_logs/`, `state/` в .gitignore
   - Это защитит конфиденциальные данные

### ШАГ 2: Настройка сервера

1. **Подключитесь к серверу:**
   ```bash
   ssh root@ваш_сервер_ip
   ```

2. **Создайте пользователя:**
   ```bash
   adduser botuser
   usermod -aG sudo botuser
   ```

3. **Настройте SSH ключи:**
   ```bash
   # На локальной машине (PowerShell)
   ssh-keygen -t ed25519 -C "bot_deployment"
   type $env:USERPROFILE\.ssh\id_ed25519.pub | ssh botuser@ваш_сервер_ip "mkdir -p ~/.ssh && cat >> ~/.ssh/authorized_keys"
   ```

4. **Подключитесь как botuser:**
   ```bash
   ssh botuser@ваш_сервер_ip
   ```

5. **Скопируйте и запустите скрипт настройки:**
   ```bash
   # На локальной машине
   scp server_setup.sh botuser@ваш_сервер_ip:~/server_setup.sh
   
   # На сервере
   chmod +x ~/server_setup.sh
   ~/server_setup.sh
   ```

### ШАГ 3: Развертывание бота

1. **Клонируйте репозиторий:**
   ```bash
   cd ~/trading_bot
   git clone https://github.com/ваш_username/trading_bot.git .
   ```

2. **Создайте виртуальное окружение:**
   ```bash
   python3.11 -m venv venv
   source venv/bin/activate
   pip install --upgrade pip
   pip install -r requirements.txt
   ```

3. **Скопируйте .env файл:**
   ```bash
   # На локальной машине
   scp "C:\Users\karim\OneDrive\Рабочий стол\Bot\.env" botuser@ваш_сервер_ip:~/trading_bot/.env
   ```

4. **Создайте systemd сервис:**
   ```bash
   # На сервере
   chmod +x ~/trading_bot/create_systemd_service.sh
   ~/trading_bot/create_systemd_service.sh
   ```

5. **Запустите бота:**
   ```bash
   sudo systemctl start trading-bot.service
   sudo systemctl status trading-bot.service
   ```

### ШАГ 4: Настройка автоматизации

1. **Настройте скрипты обновления и бэкапа:**
   ```bash
   chmod +x ~/trading_bot/server_update.sh
   chmod +x ~/trading_bot/server_backup.sh
   ```

2. **Настройте cron:**
   ```bash
   crontab -e
   # Добавьте строки:
   # Обновление каждые 6 часов
   0 */6 * * * /home/botuser/trading_bot/server_update.sh >> /home/botuser/trading_bot/logs/update.log 2>&1
   # Резервное копирование каждый день в 3:00
   0 3 * * * /home/botuser/trading_bot/server_backup.sh
   ```

---

## 📝 ЕЖЕДНЕВНАЯ РАБОТА

### Обновление кода:

1. **На локальной машине:**
   - Внесите изменения в код
   - Запустите `sync_to_server.bat`
   - Код автоматически отправится в Git

2. **На сервере:**
   - Автоматически обновится через 6 часов (через cron)
   - Или вручную: `~/trading_bot/server_update.sh`

### Резервное копирование:

- **Автоматически:** каждый день в 3:00 на сервере
- **Вручную:** запустите `backup_from_server.bat` на локальной машине

---

## 🔧 УПРАВЛЕНИЕ БОТОМ НА СЕРВЕРЕ

### Полезные команды:

```bash
# Статус сервиса
sudo systemctl status trading-bot.service

# Перезапуск
sudo systemctl restart trading-bot.service

# Остановка
sudo systemctl stop trading-bot.service

# Просмотр логов (systemd)
sudo journalctl -u trading-bot.service -f

# Просмотр логов (файл)
tail -f ~/trading_bot/logs/trading_bot.log

# Просмотр логов ошибок
tail -f ~/trading_bot/logs/bot_error.log

# Проверка использования ресурсов
htop
```

---

## ✅ ПРОВЕРКА РАБОТОСПОСОБНОСТИ

1. **Проверьте статус сервиса:**
   ```bash
   sudo systemctl status trading-bot.service
   # Должно быть: active (running)
   ```

2. **Проверьте логи:**
   ```bash
   tail -n 50 ~/trading_bot/logs/trading_bot.log
   ```

3. **Проверьте через Telegram:**
   - Отправьте `/status` боту
   - Бот должен ответить

---

## 🛡️ БЕЗОПАСНОСТЬ

1. **Защита .env файла:**
   ```bash
   chmod 600 ~/trading_bot/.env
   ```

2. **Настройка файрвола:**
   ```bash
   sudo apt install -y ufw
   sudo ufw allow 22/tcp
   sudo ufw enable
   ```

3. **Регулярные обновления:**
   ```bash
   sudo apt install -y unattended-upgrades
   ```

---

## 📊 МОНИТОРИНГ

1. **Через Telegram:**
   - Бот отправляет уведомления о сделках
   - Команда `/status` показывает текущий статус

2. **На сервере:**
   ```bash
   # Мониторинг ресурсов
   htop
   
   # Мониторинг логов
   tail -f ~/trading_bot/logs/trading_bot.log
   ```

---

## 🔄 СИНХРОНИЗАЦИЯ КОДА

### Процесс:

1. **Разработка на локальной машине:**
   - Вносите изменения
   - Тестируйте локально
   - Коммитьте в Git

2. **Синхронизация:**
   - Запустите `sync_to_server.bat`
   - Код отправится в GitHub

3. **Обновление на сервере:**
   - Автоматически через 6 часов
   - Или вручную: `~/trading_bot/server_update.sh`

---

## 📦 СТРУКТУРА ФАЙЛОВ

```
Локальная машина:
├── Bot/
│   ├── main.py
│   ├── config.py
│   ├── .env (НЕ в Git!)
│   ├── sync_to_server.bat
│   ├── backup_from_server.bat
│   └── ...

Сервер:
├── ~/trading_bot/
│   ├── main.py
│   ├── config.py
│   ├── .env (скопирован вручную)
│   ├── venv/
│   ├── logs/
│   ├── audit_logs/
│   ├── state/
│   ├── server_update.sh
│   ├── server_backup.sh
│   └── ...
```

---

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

1. **.env файл НЕ должен быть в Git!**
   - Убедитесь, что он в .gitignore
   - Копируйте вручную на сервер

2. **Резервное копирование:**
   - Настройте автоматическое копирование
   - Регулярно проверяйте бэкапы

3. **Мониторинг:**
   - Регулярно проверяйте логи
   - Настройте алерты через Telegram

4. **Безопасность:**
   - Используйте SSH ключи
   - Защитите .env файл
   - Настройте файрвол

---

## 🆘 РЕШЕНИЕ ПРОБЛЕМ

### Бот не запускается:

```bash
# Проверьте логи
sudo journalctl -u trading-bot.service -n 50

# Проверьте права доступа
ls -la ~/trading_bot/.env

# Проверьте виртуальное окружение
source ~/trading_bot/venv/bin/activate
python --version
```

### Бот падает:

```bash
# Проверьте логи ошибок
tail -f ~/trading_bot/logs/bot_error.log

# Проверьте использование ресурсов
htop
```

### Проблемы с синхронизацией:

```bash
# Проверьте Git статус
cd ~/trading_bot
git status

# Принудительное обновление
git fetch origin
git reset --hard origin/main
```

---

## 📞 ПОДДЕРЖКА

При возникновении проблем:
1. Проверьте логи на сервере
2. Проверьте статус сервиса
3. Проверьте подключение к API и Telegram
4. При необходимости перезапустите сервис

---

## ✅ ЧЕКЛИСТ РАЗВЕРТЫВАНИЯ

- [ ] Выбран и настроен VPS сервер
- [ ] Создан пользователь botuser
- [ ] Настроены SSH ключи
- [ ] Установлен Python и зависимости
- [ ] Код синхронизирован через Git
- [ ] Создан .env файл на сервере
- [ ] Бот запущен как systemd сервис
- [ ] Настроена автоматическая синхронизация
- [ ] Настроено резервное копирование
- [ ] Настроен файрвол
- [ ] Проверена работоспособность
- [ ] Настроен мониторинг

---

**Готово! Бот теперь работает на удаленном сервере 24/7!** 🚀


## 📋 Что было создано

### Документация:
1. **РАЗВЕРТЫВАНИЕ_НА_СЕРВЕРЕ.md** - Полная подробная инструкция (11 этапов)
2. **БЫСТРЫЙ_СТАРТ_СЕРВЕР.md** - Краткая инструкция для быстрого старта

### Скрипты для локальной машины (Windows):
1. **sync_to_server.bat** - Синхронизация кода с сервером через Git
2. **backup_from_server.bat** - Резервное копирование данных с сервера

### Скрипты для сервера (Linux):
1. **server_setup.sh** - Первоначальная настройка сервера
2. **server_update.sh** - Обновление бота на сервере
3. **server_backup.sh** - Резервное копирование на сервере
4. **create_systemd_service.sh** - Создание systemd сервиса

### Конфигурация:
1. **.gitignore** - Исключение конфиденциальных файлов из Git

---

## 🚀 БЫСТРЫЙ СТАРТ (пошагово)

### ШАГ 1: Подготовка на локальной машине

1. **Инициализируйте Git репозиторий:**
   ```bash
   cd "C:\Users\karim\OneDrive\Рабочий стол\Bot"
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   ```

2. **Создайте репозиторий на GitHub:**
   - Зайдите на https://github.com
   - Создайте новый приватный репозиторий (например, `trading_bot`)
   - Добавьте remote:
   ```bash
   git remote add origin https://github.com/ваш_username/trading_bot.git
   git push -u origin main
   ```

3. **Проверьте .gitignore:**
   - Убедитесь, что `.env`, `*.log`, `audit_logs/`, `state/` в .gitignore
   - Это защитит конфиденциальные данные

### ШАГ 2: Настройка сервера

1. **Подключитесь к серверу:**
   ```bash
   ssh root@ваш_сервер_ip
   ```

2. **Создайте пользователя:**
   ```bash
   adduser botuser
   usermod -aG sudo botuser
   ```

3. **Настройте SSH ключи:**
   ```bash
   # На локальной машине (PowerShell)
   ssh-keygen -t ed25519 -C "bot_deployment"
   type $env:USERPROFILE\.ssh\id_ed25519.pub | ssh botuser@ваш_сервер_ip "mkdir -p ~/.ssh && cat >> ~/.ssh/authorized_keys"
   ```

4. **Подключитесь как botuser:**
   ```bash
   ssh botuser@ваш_сервер_ip
   ```

5. **Скопируйте и запустите скрипт настройки:**
   ```bash
   # На локальной машине
   scp server_setup.sh botuser@ваш_сервер_ip:~/server_setup.sh
   
   # На сервере
   chmod +x ~/server_setup.sh
   ~/server_setup.sh
   ```

### ШАГ 3: Развертывание бота

1. **Клонируйте репозиторий:**
   ```bash
   cd ~/trading_bot
   git clone https://github.com/ваш_username/trading_bot.git .
   ```

2. **Создайте виртуальное окружение:**
   ```bash
   python3.11 -m venv venv
   source venv/bin/activate
   pip install --upgrade pip
   pip install -r requirements.txt
   ```

3. **Скопируйте .env файл:**
   ```bash
   # На локальной машине
   scp "C:\Users\karim\OneDrive\Рабочий стол\Bot\.env" botuser@ваш_сервер_ip:~/trading_bot/.env
   ```

4. **Создайте systemd сервис:**
   ```bash
   # На сервере
   chmod +x ~/trading_bot/create_systemd_service.sh
   ~/trading_bot/create_systemd_service.sh
   ```

5. **Запустите бота:**
   ```bash
   sudo systemctl start trading-bot.service
   sudo systemctl status trading-bot.service
   ```

### ШАГ 4: Настройка автоматизации

1. **Настройте скрипты обновления и бэкапа:**
   ```bash
   chmod +x ~/trading_bot/server_update.sh
   chmod +x ~/trading_bot/server_backup.sh
   ```

2. **Настройте cron:**
   ```bash
   crontab -e
   # Добавьте строки:
   # Обновление каждые 6 часов
   0 */6 * * * /home/botuser/trading_bot/server_update.sh >> /home/botuser/trading_bot/logs/update.log 2>&1
   # Резервное копирование каждый день в 3:00
   0 3 * * * /home/botuser/trading_bot/server_backup.sh
   ```

---

## 📝 ЕЖЕДНЕВНАЯ РАБОТА

### Обновление кода:

1. **На локальной машине:**
   - Внесите изменения в код
   - Запустите `sync_to_server.bat`
   - Код автоматически отправится в Git

2. **На сервере:**
   - Автоматически обновится через 6 часов (через cron)
   - Или вручную: `~/trading_bot/server_update.sh`

### Резервное копирование:

- **Автоматически:** каждый день в 3:00 на сервере
- **Вручную:** запустите `backup_from_server.bat` на локальной машине

---

## 🔧 УПРАВЛЕНИЕ БОТОМ НА СЕРВЕРЕ

### Полезные команды:

```bash
# Статус сервиса
sudo systemctl status trading-bot.service

# Перезапуск
sudo systemctl restart trading-bot.service

# Остановка
sudo systemctl stop trading-bot.service

# Просмотр логов (systemd)
sudo journalctl -u trading-bot.service -f

# Просмотр логов (файл)
tail -f ~/trading_bot/logs/trading_bot.log

# Просмотр логов ошибок
tail -f ~/trading_bot/logs/bot_error.log

# Проверка использования ресурсов
htop
```

---

## ✅ ПРОВЕРКА РАБОТОСПОСОБНОСТИ

1. **Проверьте статус сервиса:**
   ```bash
   sudo systemctl status trading-bot.service
   # Должно быть: active (running)
   ```

2. **Проверьте логи:**
   ```bash
   tail -n 50 ~/trading_bot/logs/trading_bot.log
   ```

3. **Проверьте через Telegram:**
   - Отправьте `/status` боту
   - Бот должен ответить

---

## 🛡️ БЕЗОПАСНОСТЬ

1. **Защита .env файла:**
   ```bash
   chmod 600 ~/trading_bot/.env
   ```

2. **Настройка файрвола:**
   ```bash
   sudo apt install -y ufw
   sudo ufw allow 22/tcp
   sudo ufw enable
   ```

3. **Регулярные обновления:**
   ```bash
   sudo apt install -y unattended-upgrades
   ```

---

## 📊 МОНИТОРИНГ

1. **Через Telegram:**
   - Бот отправляет уведомления о сделках
   - Команда `/status` показывает текущий статус

2. **На сервере:**
   ```bash
   # Мониторинг ресурсов
   htop
   
   # Мониторинг логов
   tail -f ~/trading_bot/logs/trading_bot.log
   ```

---

## 🔄 СИНХРОНИЗАЦИЯ КОДА

### Процесс:

1. **Разработка на локальной машине:**
   - Вносите изменения
   - Тестируйте локально
   - Коммитьте в Git

2. **Синхронизация:**
   - Запустите `sync_to_server.bat`
   - Код отправится в GitHub

3. **Обновление на сервере:**
   - Автоматически через 6 часов
   - Или вручную: `~/trading_bot/server_update.sh`

---

## 📦 СТРУКТУРА ФАЙЛОВ

```
Локальная машина:
├── Bot/
│   ├── main.py
│   ├── config.py
│   ├── .env (НЕ в Git!)
│   ├── sync_to_server.bat
│   ├── backup_from_server.bat
│   └── ...

Сервер:
├── ~/trading_bot/
│   ├── main.py
│   ├── config.py
│   ├── .env (скопирован вручную)
│   ├── venv/
│   ├── logs/
│   ├── audit_logs/
│   ├── state/
│   ├── server_update.sh
│   ├── server_backup.sh
│   └── ...
```

---

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

1. **.env файл НЕ должен быть в Git!**
   - Убедитесь, что он в .gitignore
   - Копируйте вручную на сервер

2. **Резервное копирование:**
   - Настройте автоматическое копирование
   - Регулярно проверяйте бэкапы

3. **Мониторинг:**
   - Регулярно проверяйте логи
   - Настройте алерты через Telegram

4. **Безопасность:**
   - Используйте SSH ключи
   - Защитите .env файл
   - Настройте файрвол

---

## 🆘 РЕШЕНИЕ ПРОБЛЕМ

### Бот не запускается:

```bash
# Проверьте логи
sudo journalctl -u trading-bot.service -n 50

# Проверьте права доступа
ls -la ~/trading_bot/.env

# Проверьте виртуальное окружение
source ~/trading_bot/venv/bin/activate
python --version
```

### Бот падает:

```bash
# Проверьте логи ошибок
tail -f ~/trading_bot/logs/bot_error.log

# Проверьте использование ресурсов
htop
```

### Проблемы с синхронизацией:

```bash
# Проверьте Git статус
cd ~/trading_bot
git status

# Принудительное обновление
git fetch origin
git reset --hard origin/main
```

---

## 📞 ПОДДЕРЖКА

При возникновении проблем:
1. Проверьте логи на сервере
2. Проверьте статус сервиса
3. Проверьте подключение к API и Telegram
4. При необходимости перезапустите сервис

---

## ✅ ЧЕКЛИСТ РАЗВЕРТЫВАНИЯ

- [ ] Выбран и настроен VPS сервер
- [ ] Создан пользователь botuser
- [ ] Настроены SSH ключи
- [ ] Установлен Python и зависимости
- [ ] Код синхронизирован через Git
- [ ] Создан .env файл на сервере
- [ ] Бот запущен как systemd сервис
- [ ] Настроена автоматическая синхронизация
- [ ] Настроено резервное копирование
- [ ] Настроен файрвол
- [ ] Проверена работоспособность
- [ ] Настроен мониторинг

---

**Готово! Бот теперь работает на удаленном сервере 24/7!** 🚀


## 📋 Что было создано

### Документация:
1. **РАЗВЕРТЫВАНИЕ_НА_СЕРВЕРЕ.md** - Полная подробная инструкция (11 этапов)
2. **БЫСТРЫЙ_СТАРТ_СЕРВЕР.md** - Краткая инструкция для быстрого старта

### Скрипты для локальной машины (Windows):
1. **sync_to_server.bat** - Синхронизация кода с сервером через Git
2. **backup_from_server.bat** - Резервное копирование данных с сервера

### Скрипты для сервера (Linux):
1. **server_setup.sh** - Первоначальная настройка сервера
2. **server_update.sh** - Обновление бота на сервере
3. **server_backup.sh** - Резервное копирование на сервере
4. **create_systemd_service.sh** - Создание systemd сервиса

### Конфигурация:
1. **.gitignore** - Исключение конфиденциальных файлов из Git

---

## 🚀 БЫСТРЫЙ СТАРТ (пошагово)

### ШАГ 1: Подготовка на локальной машине

1. **Инициализируйте Git репозиторий:**
   ```bash
   cd "C:\Users\karim\OneDrive\Рабочий стол\Bot"
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   ```

2. **Создайте репозиторий на GitHub:**
   - Зайдите на https://github.com
   - Создайте новый приватный репозиторий (например, `trading_bot`)
   - Добавьте remote:
   ```bash
   git remote add origin https://github.com/ваш_username/trading_bot.git
   git push -u origin main
   ```

3. **Проверьте .gitignore:**
   - Убедитесь, что `.env`, `*.log`, `audit_logs/`, `state/` в .gitignore
   - Это защитит конфиденциальные данные

### ШАГ 2: Настройка сервера

1. **Подключитесь к серверу:**
   ```bash
   ssh root@ваш_сервер_ip
   ```

2. **Создайте пользователя:**
   ```bash
   adduser botuser
   usermod -aG sudo botuser
   ```

3. **Настройте SSH ключи:**
   ```bash
   # На локальной машине (PowerShell)
   ssh-keygen -t ed25519 -C "bot_deployment"
   type $env:USERPROFILE\.ssh\id_ed25519.pub | ssh botuser@ваш_сервер_ip "mkdir -p ~/.ssh && cat >> ~/.ssh/authorized_keys"
   ```

4. **Подключитесь как botuser:**
   ```bash
   ssh botuser@ваш_сервер_ip
   ```

5. **Скопируйте и запустите скрипт настройки:**
   ```bash
   # На локальной машине
   scp server_setup.sh botuser@ваш_сервер_ip:~/server_setup.sh
   
   # На сервере
   chmod +x ~/server_setup.sh
   ~/server_setup.sh
   ```

### ШАГ 3: Развертывание бота

1. **Клонируйте репозиторий:**
   ```bash
   cd ~/trading_bot
   git clone https://github.com/ваш_username/trading_bot.git .
   ```

2. **Создайте виртуальное окружение:**
   ```bash
   python3.11 -m venv venv
   source venv/bin/activate
   pip install --upgrade pip
   pip install -r requirements.txt
   ```

3. **Скопируйте .env файл:**
   ```bash
   # На локальной машине
   scp "C:\Users\karim\OneDrive\Рабочий стол\Bot\.env" botuser@ваш_сервер_ip:~/trading_bot/.env
   ```

4. **Создайте systemd сервис:**
   ```bash
   # На сервере
   chmod +x ~/trading_bot/create_systemd_service.sh
   ~/trading_bot/create_systemd_service.sh
   ```

5. **Запустите бота:**
   ```bash
   sudo systemctl start trading-bot.service
   sudo systemctl status trading-bot.service
   ```

### ШАГ 4: Настройка автоматизации

1. **Настройте скрипты обновления и бэкапа:**
   ```bash
   chmod +x ~/trading_bot/server_update.sh
   chmod +x ~/trading_bot/server_backup.sh
   ```

2. **Настройте cron:**
   ```bash
   crontab -e
   # Добавьте строки:
   # Обновление каждые 6 часов
   0 */6 * * * /home/botuser/trading_bot/server_update.sh >> /home/botuser/trading_bot/logs/update.log 2>&1
   # Резервное копирование каждый день в 3:00
   0 3 * * * /home/botuser/trading_bot/server_backup.sh
   ```

---

## 📝 ЕЖЕДНЕВНАЯ РАБОТА

### Обновление кода:

1. **На локальной машине:**
   - Внесите изменения в код
   - Запустите `sync_to_server.bat`
   - Код автоматически отправится в Git

2. **На сервере:**
   - Автоматически обновится через 6 часов (через cron)
   - Или вручную: `~/trading_bot/server_update.sh`

### Резервное копирование:

- **Автоматически:** каждый день в 3:00 на сервере
- **Вручную:** запустите `backup_from_server.bat` на локальной машине

---

## 🔧 УПРАВЛЕНИЕ БОТОМ НА СЕРВЕРЕ

### Полезные команды:

```bash
# Статус сервиса
sudo systemctl status trading-bot.service

# Перезапуск
sudo systemctl restart trading-bot.service

# Остановка
sudo systemctl stop trading-bot.service

# Просмотр логов (systemd)
sudo journalctl -u trading-bot.service -f

# Просмотр логов (файл)
tail -f ~/trading_bot/logs/trading_bot.log

# Просмотр логов ошибок
tail -f ~/trading_bot/logs/bot_error.log

# Проверка использования ресурсов
htop
```

---

## ✅ ПРОВЕРКА РАБОТОСПОСОБНОСТИ

1. **Проверьте статус сервиса:**
   ```bash
   sudo systemctl status trading-bot.service
   # Должно быть: active (running)
   ```

2. **Проверьте логи:**
   ```bash
   tail -n 50 ~/trading_bot/logs/trading_bot.log
   ```

3. **Проверьте через Telegram:**
   - Отправьте `/status` боту
   - Бот должен ответить

---

## 🛡️ БЕЗОПАСНОСТЬ

1. **Защита .env файла:**
   ```bash
   chmod 600 ~/trading_bot/.env
   ```

2. **Настройка файрвола:**
   ```bash
   sudo apt install -y ufw
   sudo ufw allow 22/tcp
   sudo ufw enable
   ```

3. **Регулярные обновления:**
   ```bash
   sudo apt install -y unattended-upgrades
   ```

---

## 📊 МОНИТОРИНГ

1. **Через Telegram:**
   - Бот отправляет уведомления о сделках
   - Команда `/status` показывает текущий статус

2. **На сервере:**
   ```bash
   # Мониторинг ресурсов
   htop
   
   # Мониторинг логов
   tail -f ~/trading_bot/logs/trading_bot.log
   ```

---

## 🔄 СИНХРОНИЗАЦИЯ КОДА

### Процесс:

1. **Разработка на локальной машине:**
   - Вносите изменения
   - Тестируйте локально
   - Коммитьте в Git

2. **Синхронизация:**
   - Запустите `sync_to_server.bat`
   - Код отправится в GitHub

3. **Обновление на сервере:**
   - Автоматически через 6 часов
   - Или вручную: `~/trading_bot/server_update.sh`

---

## 📦 СТРУКТУРА ФАЙЛОВ

```
Локальная машина:
├── Bot/
│   ├── main.py
│   ├── config.py
│   ├── .env (НЕ в Git!)
│   ├── sync_to_server.bat
│   ├── backup_from_server.bat
│   └── ...

Сервер:
├── ~/trading_bot/
│   ├── main.py
│   ├── config.py
│   ├── .env (скопирован вручную)
│   ├── venv/
│   ├── logs/
│   ├── audit_logs/
│   ├── state/
│   ├── server_update.sh
│   ├── server_backup.sh
│   └── ...
```

---

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

1. **.env файл НЕ должен быть в Git!**
   - Убедитесь, что он в .gitignore
   - Копируйте вручную на сервер

2. **Резервное копирование:**
   - Настройте автоматическое копирование
   - Регулярно проверяйте бэкапы

3. **Мониторинг:**
   - Регулярно проверяйте логи
   - Настройте алерты через Telegram

4. **Безопасность:**
   - Используйте SSH ключи
   - Защитите .env файл
   - Настройте файрвол

---

## 🆘 РЕШЕНИЕ ПРОБЛЕМ

### Бот не запускается:

```bash
# Проверьте логи
sudo journalctl -u trading-bot.service -n 50

# Проверьте права доступа
ls -la ~/trading_bot/.env

# Проверьте виртуальное окружение
source ~/trading_bot/venv/bin/activate
python --version
```

### Бот падает:

```bash
# Проверьте логи ошибок
tail -f ~/trading_bot/logs/bot_error.log

# Проверьте использование ресурсов
htop
```

### Проблемы с синхронизацией:

```bash
# Проверьте Git статус
cd ~/trading_bot
git status

# Принудительное обновление
git fetch origin
git reset --hard origin/main
```

---

## 📞 ПОДДЕРЖКА

При возникновении проблем:
1. Проверьте логи на сервере
2. Проверьте статус сервиса
3. Проверьте подключение к API и Telegram
4. При необходимости перезапустите сервис

---

## ✅ ЧЕКЛИСТ РАЗВЕРТЫВАНИЯ

- [ ] Выбран и настроен VPS сервер
- [ ] Создан пользователь botuser
- [ ] Настроены SSH ключи
- [ ] Установлен Python и зависимости
- [ ] Код синхронизирован через Git
- [ ] Создан .env файл на сервере
- [ ] Бот запущен как systemd сервис
- [ ] Настроена автоматическая синхронизация
- [ ] Настроено резервное копирование
- [ ] Настроен файрвол
- [ ] Проверена работоспособность
- [ ] Настроен мониторинг

---

**Готово! Бот теперь работает на удаленном сервере 24/7!** 🚀





