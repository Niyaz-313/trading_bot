# БЫСТРАЯ ИНСТРУКЦИЯ: Подключение Git к серверу

## 🎯 Цель
Подключить Git репозиторий на сервере и скачать файлы.

---

## 📋 ШАГИ

### ШАГ 1: На локальной машине - отправить код на GitHub

```powershell
# Перейдите в директорию проекта
cd "C:\Users\karim\OneDrive\Рабочий стол\Bot"

# Проверьте статус
git status

# Если репозиторий не инициализирован:
git init
git add .
git commit -m "Initial commit"
git branch -M main

# Добавьте remote (если еще не добавлен)
git remote add origin https://github.com/Niyaz-313/trading_bot.git

# Отправьте код
git push -u origin main
```

**Или используйте готовый скрипт:** `БЫСТРОЕ_ПОДКЛЮЧЕНИЕ_GIT.bat`

---

### ШАГ 2: На сервере - подключиться и клонировать

```bash
# 1. Подключитесь к серверу (замените YOUR_IPv4_ADDRESS на ваш IPv4 адрес)
ssh botuser@YOUR_IPv4_ADDRESS
# Пример: ssh botuser@192.168.1.100

# 2. Перейдите в домашнюю директорию
cd ~

# 3. Клонируйте репозиторий
git clone https://github.com/Niyaz-313/trading_bot.git trading_bot

# 4. Перейдите в директорию
cd trading_bot
```

---

### ШАГ 3: На сервере - установить зависимости

```bash
# Создайте виртуальное окружение
python3 -m venv venv

# Активируйте виртуальное окружение
source venv/bin/activate

# Обновите pip
pip install --upgrade pip

# Установите зависимости
pip install -r requirements.txt
```

---

### ШАГ 4: На локальной машине - скопировать .env файл

```powershell
# Скопируйте .env файл на сервер (замените YOUR_IPv4_ADDRESS)
scp "C:\Users\karim\OneDrive\Рабочий стол\Bot\.env" botuser@YOUR_IPv4_ADDRESS:~/trading_bot/.env
# Пример: scp "C:\Users\karim\OneDrive\Рабочий стол\Bot\.env" botuser@192.168.1.100:~/trading_bot/.env
```

---

### ШАГ 5: На сервере - проверить работоспособность

```bash
# На сервере
cd ~/trading_bot
source venv/bin/activate

# Проверьте подключение к T-Invest API
python3 -c "from broker_api import BrokerAPI; b = BrokerAPI(paper_trading=True); print('OK' if b.client else 'FAIL')"
```

---

## 🔄 ОБНОВЛЕНИЕ КОДА НА СЕРВЕРЕ

### После изменений на локальной машине:

**На локальной машине:**
```powershell
cd "C:\Users\karim\OneDrive\Рабочий стол\Bot"
git add .
git commit -m "Описание изменений"
git push origin main
```

**На сервере:**
```bash
cd ~/trading_bot
source venv/bin/activate
git pull origin main
pip install -r requirements.txt
```

---

## ⚠️ ВАЖНО

1. **IPv4 адрес указывается БЕЗ квадратных скобок** (в отличие от IPv6)
2. **Если репозиторий приватный**, настройте авторизацию:
   - Personal Access Token для HTTPS
   - Или SSH ключи для SSH
3. **Файл .env НЕ должен быть в Git** (должен быть в .gitignore)

---

## 📝 ПРИМЕРЫ КОМАНД

### Подключение к серверу:
```powershell
ssh botuser@192.168.1.100
```

### Клонирование репозитория:
```bash
git clone https://github.com/Niyaz-313/trading_bot.git trading_bot
```

### Копирование .env файла:
```powershell
scp "C:\Users\karim\OneDrive\Рабочий стол\Bot\.env" botuser@192.168.1.100:~/trading_bot/.env
```

### Обновление кода на сервере:
```bash
cd ~/trading_bot
git pull origin main
```

---

## ✅ ЧЕКЛИСТ

- [ ] Код отправлен на GitHub с локальной машины
- [ ] Подключение к серверу по SSH работает
- [ ] Репозиторий клонирован на сервер
- [ ] Виртуальное окружение создано
- [ ] Зависимости установлены
- [ ] .env файл скопирован на сервер
- [ ] Проверка работоспособности пройдена

---

**Готово!** Теперь код синхронизирован между локальной машиной и сервером.

