# ШАГИ ПОДКЛЮЧЕНИЯ GIT К СЕРВЕРУ

## 🎯 Цель
Подключить Git репозиторий на сервере и скачать файлы из него.

## 📍 Путь к проекту на сервере
**Важно:** Все файлы проекта находятся в директории:
```
/home/botuser/trading_bot/trading_bot/
```

Все команды на сервере выполняются из этой директории.

---

## 📋 ПОШАГОВАЯ ИНСТРУКЦИЯ

### ШАГ 1: На локальной машине - подготовка Git репозитория

```powershell
# 1. Перейдите в директорию проекта
cd "C:\Users\karim\OneDrive\Рабочий стол\Bot"

# 2. Проверьте, инициализирован ли Git
git status

# 3. Если Git не инициализирован, выполните:
git init
git add .
git commit -m "Initial commit"
git branch -M main

# 4. Добавьте remote репозиторий (если еще не добавлен)
git remote add origin https://github.com/Niyaz-313/trading_bot.git

# 5. Проверьте remote
git remote -v

# 6. Отправьте код на GitHub
git push -u origin main
```

**Или используйте готовый скрипт:** `БЫСТРОЕ_ПОДКЛЮЧЕНИЕ_GIT.bat`

---

### ШАГ 2: На сервере - подключение и клонирование

```bash
# 1. Подключитесь к серверу (замените YOUR_IPv4_ADDRESS на ваш IPv4 адрес)
ssh botuser@YOUR_IPv4_ADDRESS
# Пример: ssh botuser@192.168.1.100

# 2. Перейдите в домашнюю директорию
cd ~

# 3. Создайте директорию для проекта (если еще не создана)
mkdir -p trading_bot

# 4. Перейдите в директорию
cd trading_bot

# 5. Клонируйте репозиторий (файлы будут в trading_bot/trading_bot/)
git clone https://github.com/Niyaz-313/trading_bot.git trading_bot

# 6. Перейдите в директорию с файлами проекта
cd /home/botuser/trading_bot/trading_bot

# 7. Проверьте, что файлы скопированы
ls -la
```

---

### ШАГ 3: На сервере - установка зависимостей

```bash
# 1. Перейдите в директорию с файлами проекта
cd /home/botuser/trading_bot/trading_bot

# 2. Создайте виртуальное окружение
python3 -m venv venv

# 3. Активируйте виртуальное окружение
source venv/bin/activate

# 4. Обновите pip
pip install --upgrade pip

# 5. Установите зависимости
pip install -r requirements.txt
```

---

### ШАГ 4: На локальной машине - копирование .env файла

```powershell
# Скопируйте .env файл на сервер (замените YOUR_IPv4_ADDRESS)
# Файл будет скопирован в /home/botuser/trading_bot/trading_bot/.env
scp "C:\Users\karim\OneDrive\Рабочий стол\Bot\.env" botuser@193.160.208.128:/home/botuser/trading_bot/trading_bot/.env
# Пример: scp "C:\Users\karim\OneDrive\Рабочий стол\Bot\.env" botuser@192.168.1.100:/home/botuser/trading_bot/trading_bot/.env
```

**Или используйте готовый скрипт:** `copy_to_server.ps1` (предварительно укажите IPv4 адрес в файле)

---

### ШАГ 5: На сервере - проверка работоспособности

```bash
# 1. Перейдите в директорию с файлами проекта
cd /home/botuser/trading_bot/trading_bot

# 2. Активируйте виртуальное окружение
source venv/bin/activate

# 3. Проверьте подключение к T-Invest API
python3 -c "from broker_api import BrokerAPI; b = BrokerAPI(paper_trading=True); print('OK' if b.client else 'FAIL')"

# 4. Проверьте Telegram бота
python3 -c "from telegram_bot import TelegramBot; t = TelegramBot(); print('OK')"
```

---

## 🔄 ОБНОВЛЕНИЕ КОДА НА СЕРВЕРЕ

### После изменений на локальной машине:

**На локальной машине:**
```powershell
cd "C:\Users\karim\OneDrive\Рабочий стол\Bot"
git add .
git commit -m "Описание изменений"
git push origin main
```

**На сервере:**
```bash
cd /home/botuser/trading_bot/trading_bot
source venv/bin/activate
git pull origin main
pip install -r requirements.txt
```

---

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

1. **Путь к проекту на сервере:** `/home/botuser/trading_bot/trading_bot/`
   - Все команды выполняются из этой директории
   - Виртуальное окружение создается в этой директории

2. **IPv4 адрес указывается БЕЗ квадратных скобок** (в отличие от IPv6)
   - Правильно: `ssh botuser@192.168.1.100`
   - Неправильно: `ssh botuser@[192.168.1.100]`

3. **Если репозиторий приватный**, настройте авторизацию:
   - **Personal Access Token** для HTTPS
   - **SSH ключи** для SSH (рекомендуется)

4. **Файл .env НЕ должен быть в Git** (должен быть в .gitignore)

5. **Структура директорий:**
   ```
   /home/botuser/
   └── trading_bot/
       └── trading_bot/          ← Здесь находятся файлы проекта
           ├── main.py
           ├── requirements.txt
           ├── .env
           ├── venv/
           └── ...
   ```

---

## 📝 ПРИМЕРЫ КОМАНД

### Подключение к серверу:
```powershell
ssh botuser@192.168.1.100
```

### Клонирование репозитория:
```bash
git clone https://github.com/Niyaz-313/trading_bot.git trading_bot
```

### Копирование .env файла:
```powershell
scp "C:\Users\karim\OneDrive\Рабочий стол\Bot\.env" botuser@192.168.1.100:/home/botuser/trading_bot/trading_bot/.env
```

### Обновление кода на сервере:
```bash
cd /home/botuser/trading_bot/trading_bot
git pull origin main
```

---

## ✅ ЧЕКЛИСТ

- [ ] Git репозиторий создан на GitHub
- [ ] Код отправлен на GitHub с локальной машины
- [ ] Подключение к серверу по SSH работает
- [ ] Git установлен на сервере
- [ ] Репозиторий клонирован на сервер
- [ ] Виртуальное окружение создано
- [ ] Зависимости установлены
- [ ] .env файл скопирован на сервер
- [ ] Проверка работоспособности пройдена

---

## 🚀 БЫСТРЫЙ СТАРТ

**На локальной машине:**
```powershell
cd "C:\Users\karim\OneDrive\Рабочий стол\Bot"
.\БЫСТРОЕ_ПОДКЛЮЧЕНИЕ_GIT.bat
```

**На сервере:**
```bash
ssh botuser@YOUR_IPv4_ADDRESS
cd ~
mkdir -p trading_bot
cd trading_bot
git clone https://github.com/Niyaz-313/trading_bot.git trading_bot
cd /home/botuser/trading_bot/trading_bot
python3 -m venv venv
source venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt
```

**Скопируйте .env:**
```powershell
scp "C:\Users\karim\OneDrive\Рабочий стол\Bot\.env" botuser@YOUR_IPv4_ADDRESS:/home/botuser/trading_bot/trading_bot/.env
```

**Готово!** Теперь код синхронизирован между локальной машиной и сервером.

