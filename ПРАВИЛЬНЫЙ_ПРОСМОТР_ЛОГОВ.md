# ПРАВИЛЬНЫЙ ПРОСМОТР ЛОГОВ БОТА

## ⚠️ ВАЖНО: Почему в journalctl нет детальных логов?

**`journalctl`** показывает только **системные сообщения** о запуске/остановке сервиса.

**Детальные логи бота** (свечи, решения о покупках/продажах) записываются в **файлы**, а не в systemd journal.

---

## 📁 ГДЕ НАХОДЯТСЯ ЛОГИ БОТА

### Основные файлы логов:

1. **`logs/trading_bot.log`** - все логи бота (свечи, решения, сделки)
2. **`logs/bot_output.log`** - стандартный вывод (stdout) из systemd
3. **`logs/bot_error.log`** - ошибки (stderr) из systemd
4. **`audit_logs/trades_audit.jsonl`** - детальный аудит всех решений и сделок

---

## ✅ ПРАВИЛЬНЫЕ КОМАНДЫ ДЛЯ ПРОСМОТРА ЛОГОВ

### 1. Просмотр основного лога в реальном времени (РЕКОМЕНДУЕТСЯ)

```bash
cd /home/botuser/trading_bot/trading_bot
tail -f logs/trading_bot.log
```

**Эта команда покажет:**
- Получение данных о свечах
- Анализ символов
- Решения о покупках/продажах
- Размещение ордеров
- Все действия бота

---

### 2. Просмотр логов ошибок

```bash
cd /home/botuser/trading_bot/trading_bot
tail -f logs/bot_error.log
```

---

### 3. Просмотр стандартного вывода (stdout)

```bash
cd /home/botuser/trading_bot/trading_bot
tail -f logs/bot_output.log
```

---

### 4. Просмотр аудита в реальном времени

```bash
cd /home/botuser/trading_bot/trading_bot
tail -f audit_logs/trades_audit.jsonl
```

---

### 5. Просмотр последних 100 строк основного лога

```bash
cd /home/botuser/trading_bot/trading_bot
tail -n 100 logs/trading_bot.log
```

---

### 6. Просмотр всех логов одновременно (в разных терминалах)

**Терминал 1:**
```bash
cd /home/botuser/trading_bot/trading_bot
tail -f logs/trading_bot.log
```

**Терминал 2:**
```bash
cd /home/botuser/trading_bot/trading_bot
tail -f logs/bot_error.log
```

**Терминал 3:**
```bash
cd /home/botuser/trading_bot/trading_bot
tail -f audit_logs/trades_audit.jsonl
```

---

## 🔍 ПРОВЕРКА, ЧТО БОТ РАБОТАЕТ

### 1. Проверка статуса сервиса

```bash
sudo systemctl status trading-bot.service --no-pager
```

Должно быть: `Active: active (running)`

---

### 2. Проверка, что файлы логов создаются

```bash
cd /home/botuser/trading_bot/trading_bot
ls -lh logs/
ls -lh audit_logs/
```

Если файлов нет - бот не запущен или не может писать логи.

---

### 3. Проверка последних записей в логе

```bash
cd /home/botuser/trading_bot/trading_bot
tail -n 50 logs/trading_bot.log
```

Если файл пустой или старый - бот не работает или не пишет логи.

---

## 🚨 ЕСЛИ ЛОГИ НЕ СОЗДАЮТСЯ

### Проверка прав доступа

```bash
cd /home/botuser/trading_bot/trading_bot
ls -la logs/
ls -la audit_logs/
```

Если директории не существуют, создайте их:

```bash
cd /home/botuser/trading_bot/trading_bot
mkdir -p logs audit_logs
chown -R botuser:botuser logs audit_logs
```

---

### Проверка, что бот запущен

```bash
sudo systemctl status trading-bot.service
ps aux | grep python | grep main.py
```

---

## 📊 ПОИСК В ЛОГАХ

### Поиск конкретного символа

```bash
cd /home/botuser/trading_bot/trading_bot
grep "SBER" logs/trading_bot.log | tail -20
```

### Поиск ошибок

```bash
cd /home/botuser/trading_bot/trading_bot
grep -i error logs/trading_bot.log | tail -20
```

### Поиск покупок

```bash
cd /home/botuser/trading_bot/trading_bot
grep -i "buy\|покупка" logs/trading_bot.log | tail -20
```

### Поиск продаж

```bash
cd /home/botuser/trading_bot/trading_bot
grep -i "sell\|продажа" logs/trading_bot.log | tail -20
```

---

## ✅ РЕКОМЕНДУЕМАЯ КОМАНДА ДЛЯ МОНИТОРИНГА

**Для постоянного мониторинга используйте:**

```bash
cd /home/botuser/trading_bot/trading_bot && tail -f logs/trading_bot.log
```

Эта команда покажет все действия бота в реальном времени:
- Получение данных о свечах
- Анализ каждого символа
- Решения о покупках/продажах
- Размещение ордеров
- Все ошибки и предупреждения

---

## 🔄 ВЫХОД ИЗ ПРОСМОТРА

**Для выхода из `tail -f`:** Нажмите `Ctrl+C`

---

## 📝 ПРИМЕРЫ ТОГО, ЧТО ВЫ УВИДИТЕ В ЛОГАХ

```
2026-01-22 00:17:40 - __main__ - INFO - ============================================================
2026-01-22 00:17:40 - __main__ - INFO - Цикл #1 начат
2026-01-22 00:17:40 - __main__ - INFO - Получение исторических данных для SBER...
2026-01-22 00:17:41 - tinvest_api - INFO - Получено 100 свечей для SBER
2026-01-22 00:17:41 - trading_strategy - INFO - Анализ SBER: RSI=45.2, MACD=-0.5, confidence=0.65
2026-01-22 00:17:41 - __main__ - INFO - Решение для SBER: BUY (confidence=0.65)
2026-01-22 00:17:42 - __main__ - INFO - Размещение ордера BUY 10 лотов SBER...
2026-01-22 00:17:43 - __main__ - INFO - Ордер размещен успешно
```

Это и есть детальные логи работы бота!


## ⚠️ ВАЖНО: Почему в journalctl нет детальных логов?

**`journalctl`** показывает только **системные сообщения** о запуске/остановке сервиса.

**Детальные логи бота** (свечи, решения о покупках/продажах) записываются в **файлы**, а не в systemd journal.

---

## 📁 ГДЕ НАХОДЯТСЯ ЛОГИ БОТА

### Основные файлы логов:

1. **`logs/trading_bot.log`** - все логи бота (свечи, решения, сделки)
2. **`logs/bot_output.log`** - стандартный вывод (stdout) из systemd
3. **`logs/bot_error.log`** - ошибки (stderr) из systemd
4. **`audit_logs/trades_audit.jsonl`** - детальный аудит всех решений и сделок

---

## ✅ ПРАВИЛЬНЫЕ КОМАНДЫ ДЛЯ ПРОСМОТРА ЛОГОВ

### 1. Просмотр основного лога в реальном времени (РЕКОМЕНДУЕТСЯ)

```bash
cd /home/botuser/trading_bot/trading_bot
tail -f logs/trading_bot.log
```

**Эта команда покажет:**
- Получение данных о свечах
- Анализ символов
- Решения о покупках/продажах
- Размещение ордеров
- Все действия бота

---

### 2. Просмотр логов ошибок

```bash
cd /home/botuser/trading_bot/trading_bot
tail -f logs/bot_error.log
```

---

### 3. Просмотр стандартного вывода (stdout)

```bash
cd /home/botuser/trading_bot/trading_bot
tail -f logs/bot_output.log
```

---

### 4. Просмотр аудита в реальном времени

```bash
cd /home/botuser/trading_bot/trading_bot
tail -f audit_logs/trades_audit.jsonl
```

---

### 5. Просмотр последних 100 строк основного лога

```bash
cd /home/botuser/trading_bot/trading_bot
tail -n 100 logs/trading_bot.log
```

---

### 6. Просмотр всех логов одновременно (в разных терминалах)

**Терминал 1:**
```bash
cd /home/botuser/trading_bot/trading_bot
tail -f logs/trading_bot.log
```

**Терминал 2:**
```bash
cd /home/botuser/trading_bot/trading_bot
tail -f logs/bot_error.log
```

**Терминал 3:**
```bash
cd /home/botuser/trading_bot/trading_bot
tail -f audit_logs/trades_audit.jsonl
```

---

## 🔍 ПРОВЕРКА, ЧТО БОТ РАБОТАЕТ

### 1. Проверка статуса сервиса

```bash
sudo systemctl status trading-bot.service --no-pager
```

Должно быть: `Active: active (running)`

---

### 2. Проверка, что файлы логов создаются

```bash
cd /home/botuser/trading_bot/trading_bot
ls -lh logs/
ls -lh audit_logs/
```

Если файлов нет - бот не запущен или не может писать логи.

---

### 3. Проверка последних записей в логе

```bash
cd /home/botuser/trading_bot/trading_bot
tail -n 50 logs/trading_bot.log
```

Если файл пустой или старый - бот не работает или не пишет логи.

---

## 🚨 ЕСЛИ ЛОГИ НЕ СОЗДАЮТСЯ

### Проверка прав доступа

```bash
cd /home/botuser/trading_bot/trading_bot
ls -la logs/
ls -la audit_logs/
```

Если директории не существуют, создайте их:

```bash
cd /home/botuser/trading_bot/trading_bot
mkdir -p logs audit_logs
chown -R botuser:botuser logs audit_logs
```

---

### Проверка, что бот запущен

```bash
sudo systemctl status trading-bot.service
ps aux | grep python | grep main.py
```

---

## 📊 ПОИСК В ЛОГАХ

### Поиск конкретного символа

```bash
cd /home/botuser/trading_bot/trading_bot
grep "SBER" logs/trading_bot.log | tail -20
```

### Поиск ошибок

```bash
cd /home/botuser/trading_bot/trading_bot
grep -i error logs/trading_bot.log | tail -20
```

### Поиск покупок

```bash
cd /home/botuser/trading_bot/trading_bot
grep -i "buy\|покупка" logs/trading_bot.log | tail -20
```

### Поиск продаж

```bash
cd /home/botuser/trading_bot/trading_bot
grep -i "sell\|продажа" logs/trading_bot.log | tail -20
```

---

## ✅ РЕКОМЕНДУЕМАЯ КОМАНДА ДЛЯ МОНИТОРИНГА

**Для постоянного мониторинга используйте:**

```bash
cd /home/botuser/trading_bot/trading_bot && tail -f logs/trading_bot.log
```

Эта команда покажет все действия бота в реальном времени:
- Получение данных о свечах
- Анализ каждого символа
- Решения о покупках/продажах
- Размещение ордеров
- Все ошибки и предупреждения

---

## 🔄 ВЫХОД ИЗ ПРОСМОТРА

**Для выхода из `tail -f`:** Нажмите `Ctrl+C`

---

## 📝 ПРИМЕРЫ ТОГО, ЧТО ВЫ УВИДИТЕ В ЛОГАХ

```
2026-01-22 00:17:40 - __main__ - INFO - ============================================================
2026-01-22 00:17:40 - __main__ - INFO - Цикл #1 начат
2026-01-22 00:17:40 - __main__ - INFO - Получение исторических данных для SBER...
2026-01-22 00:17:41 - tinvest_api - INFO - Получено 100 свечей для SBER
2026-01-22 00:17:41 - trading_strategy - INFO - Анализ SBER: RSI=45.2, MACD=-0.5, confidence=0.65
2026-01-22 00:17:41 - __main__ - INFO - Решение для SBER: BUY (confidence=0.65)
2026-01-22 00:17:42 - __main__ - INFO - Размещение ордера BUY 10 лотов SBER...
2026-01-22 00:17:43 - __main__ - INFO - Ордер размещен успешно
```

Это и есть детальные логи работы бота!


## ⚠️ ВАЖНО: Почему в journalctl нет детальных логов?

**`journalctl`** показывает только **системные сообщения** о запуске/остановке сервиса.

**Детальные логи бота** (свечи, решения о покупках/продажах) записываются в **файлы**, а не в systemd journal.

---

## 📁 ГДЕ НАХОДЯТСЯ ЛОГИ БОТА

### Основные файлы логов:

1. **`logs/trading_bot.log`** - все логи бота (свечи, решения, сделки)
2. **`logs/bot_output.log`** - стандартный вывод (stdout) из systemd
3. **`logs/bot_error.log`** - ошибки (stderr) из systemd
4. **`audit_logs/trades_audit.jsonl`** - детальный аудит всех решений и сделок

---

## ✅ ПРАВИЛЬНЫЕ КОМАНДЫ ДЛЯ ПРОСМОТРА ЛОГОВ

### 1. Просмотр основного лога в реальном времени (РЕКОМЕНДУЕТСЯ)

```bash
cd /home/botuser/trading_bot/trading_bot
tail -f logs/trading_bot.log
```

**Эта команда покажет:**
- Получение данных о свечах
- Анализ символов
- Решения о покупках/продажах
- Размещение ордеров
- Все действия бота

---

### 2. Просмотр логов ошибок

```bash
cd /home/botuser/trading_bot/trading_bot
tail -f logs/bot_error.log
```

---

### 3. Просмотр стандартного вывода (stdout)

```bash
cd /home/botuser/trading_bot/trading_bot
tail -f logs/bot_output.log
```

---

### 4. Просмотр аудита в реальном времени

```bash
cd /home/botuser/trading_bot/trading_bot
tail -f audit_logs/trades_audit.jsonl
```

---

### 5. Просмотр последних 100 строк основного лога

```bash
cd /home/botuser/trading_bot/trading_bot
tail -n 100 logs/trading_bot.log
```

---

### 6. Просмотр всех логов одновременно (в разных терминалах)

**Терминал 1:**
```bash
cd /home/botuser/trading_bot/trading_bot
tail -f logs/trading_bot.log
```

**Терминал 2:**
```bash
cd /home/botuser/trading_bot/trading_bot
tail -f logs/bot_error.log
```

**Терминал 3:**
```bash
cd /home/botuser/trading_bot/trading_bot
tail -f audit_logs/trades_audit.jsonl
```

---

## 🔍 ПРОВЕРКА, ЧТО БОТ РАБОТАЕТ

### 1. Проверка статуса сервиса

```bash
sudo systemctl status trading-bot.service --no-pager
```

Должно быть: `Active: active (running)`

---

### 2. Проверка, что файлы логов создаются

```bash
cd /home/botuser/trading_bot/trading_bot
ls -lh logs/
ls -lh audit_logs/
```

Если файлов нет - бот не запущен или не может писать логи.

---

### 3. Проверка последних записей в логе

```bash
cd /home/botuser/trading_bot/trading_bot
tail -n 50 logs/trading_bot.log
```

Если файл пустой или старый - бот не работает или не пишет логи.

---

## 🚨 ЕСЛИ ЛОГИ НЕ СОЗДАЮТСЯ

### Проверка прав доступа

```bash
cd /home/botuser/trading_bot/trading_bot
ls -la logs/
ls -la audit_logs/
```

Если директории не существуют, создайте их:

```bash
cd /home/botuser/trading_bot/trading_bot
mkdir -p logs audit_logs
chown -R botuser:botuser logs audit_logs
```

---

### Проверка, что бот запущен

```bash
sudo systemctl status trading-bot.service
ps aux | grep python | grep main.py
```

---

## 📊 ПОИСК В ЛОГАХ

### Поиск конкретного символа

```bash
cd /home/botuser/trading_bot/trading_bot
grep "SBER" logs/trading_bot.log | tail -20
```

### Поиск ошибок

```bash
cd /home/botuser/trading_bot/trading_bot
grep -i error logs/trading_bot.log | tail -20
```

### Поиск покупок

```bash
cd /home/botuser/trading_bot/trading_bot
grep -i "buy\|покупка" logs/trading_bot.log | tail -20
```

### Поиск продаж

```bash
cd /home/botuser/trading_bot/trading_bot
grep -i "sell\|продажа" logs/trading_bot.log | tail -20
```

---

## ✅ РЕКОМЕНДУЕМАЯ КОМАНДА ДЛЯ МОНИТОРИНГА

**Для постоянного мониторинга используйте:**

```bash
cd /home/botuser/trading_bot/trading_bot && tail -f logs/trading_bot.log
```

Эта команда покажет все действия бота в реальном времени:
- Получение данных о свечах
- Анализ каждого символа
- Решения о покупках/продажах
- Размещение ордеров
- Все ошибки и предупреждения

---

## 🔄 ВЫХОД ИЗ ПРОСМОТРА

**Для выхода из `tail -f`:** Нажмите `Ctrl+C`

---

## 📝 ПРИМЕРЫ ТОГО, ЧТО ВЫ УВИДИТЕ В ЛОГАХ

```
2026-01-22 00:17:40 - __main__ - INFO - ============================================================
2026-01-22 00:17:40 - __main__ - INFO - Цикл #1 начат
2026-01-22 00:17:40 - __main__ - INFO - Получение исторических данных для SBER...
2026-01-22 00:17:41 - tinvest_api - INFO - Получено 100 свечей для SBER
2026-01-22 00:17:41 - trading_strategy - INFO - Анализ SBER: RSI=45.2, MACD=-0.5, confidence=0.65
2026-01-22 00:17:41 - __main__ - INFO - Решение для SBER: BUY (confidence=0.65)
2026-01-22 00:17:42 - __main__ - INFO - Размещение ордера BUY 10 лотов SBER...
2026-01-22 00:17:43 - __main__ - INFO - Ордер размещен успешно
```

Это и есть детальные логи работы бота!




