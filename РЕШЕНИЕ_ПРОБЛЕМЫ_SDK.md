# 🔧 Решение проблемы с T-Invest SDK

## ❌ Проблема

Установлен пакет `tinkoff-invest` (альтернативный), который импортируется как `tinkoff_invest`, но код пытается использовать официальный SDK `tinkoff.invest`, который не установлен.

## ✅ Решение

Код обновлен для поддержки обоих вариантов:
1. Официальный SDK `tinkoff.invest` (если установлен)
2. Альтернативный пакет `tinkoff_invest` (установлен сейчас)

## 📋 Что нужно сделать

### Вариант 1: Использовать установленный пакет (РЕКОМЕНДУЕТСЯ)

Код уже обновлен для работы с `tinkoff_invest`. Просто убедитесь, что:
1. Пакет установлен: `pip install tinkoff-invest` ✅
2. Токен настроен в `.env`: `TINVEST_TOKEN=t.ваш_токен` ✅
3. Запустите: `python backtest.py`

### Вариант 2: Установить официальный SDK

Если хотите использовать официальный SDK:
```bash
pip uninstall tinkoff-invest
pip install invest-python
```

Но официальный SDK может иметь другое имя пакета. Проверьте документацию T-Invest API.

## ⚠️ Важно

Текущий код поддерживает оба варианта автоматически. Просто убедитесь, что:
- Пакет `tinkoff-invest` установлен
- Токен настроен в `.env`
- Токен является реальным (не примерным)

Все должно работать!



