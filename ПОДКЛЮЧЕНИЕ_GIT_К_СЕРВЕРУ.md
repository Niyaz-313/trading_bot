# ПОДКЛЮЧЕНИЕ GIT РЕПОЗИТОРИЯ К СЕРВЕРУ

## 🎯 Цель
Подключить Git репозиторий на сервере и скачать файлы из него.

---

## ЭТАП 1: ПОДГОТОВКА НА ЛОКАЛЬНОЙ МАШИНЕ

### Шаг 1: Проверка Git репозитория

```powershell
# Перейдите в директорию проекта
cd "C:\Users\karim\OneDrive\Рабочий стол\Bot"

# Проверьте статус Git
git status

# Проверьте remote репозиторий
git remote -v
```

### Шаг 2: Если репозиторий не инициализирован

```powershell
# Инициализируйте Git репозиторий
git init

# Добавьте все файлы (кроме .env и других исключений)
git add .

# Создайте первый коммит
git commit -m "Initial commit"

# Переименуйте ветку в main (если нужно)
git branch -M main
```

### Шаг 3: Настройка remote репозитория

**Если у вас уже есть репозиторий на GitHub/GitLab:**

```powershell
# Добавьте remote (если еще не добавлен)
git remote add origin https://github.com/Niyaz-313/trading_bot.git

# Или если уже есть, обновите URL:
git remote set-url origin https://github.com/Niyaz-313/trading_bot.git

# Проверьте
git remote -v
```

**Если репозитория еще нет:**

1. Создайте репозиторий на GitHub:
   - Перейдите на https://github.com/new
   - Название: `trading_bot`
   - Выберите **Private** (приватный)
   - Нажмите "Create repository"

2. Добавьте remote:
   ```powershell
   git remote add origin https://github.com/ваш_username/trading_bot.git
   ```

### Шаг 4: Отправка кода на GitHub

```powershell
# Отправьте код на GitHub
git push -u origin main
```

**Если потребуется авторизация:**
- Используйте Personal Access Token вместо пароля
- Или настройте SSH ключи для GitHub

---

## ЭТАП 2: ПОДКЛЮЧЕНИЕ К СЕРВЕРУ

### Шаг 1: Подключение по SSH

```powershell
# Подключитесь к серверу (замените YOUR_IPv4_ADDRESS на ваш IPv4 адрес)
ssh botuser@YOUR_IPv4_ADDRESS
```

**Пример:**
```powershell
ssh botuser@192.168.1.100
```

### Шаг 2: Проверка Git на сервере

```bash
# Проверьте, установлен ли Git
git --version

# Если не установлен, установите:
sudo apt update
sudo apt install -y git
```

---

## ЭТАП 3: КЛОНИРОВАНИЕ РЕПОЗИТОРИЯ НА СЕРВЕРЕ

### Вариант A: Клонирование в пустую директорию

```bash
# Перейдите в домашнюю директорию
cd ~

# Удалите старую директорию (если есть)
rm -rf trading_bot

# Клонируйте репозиторий
git clone https://github.com/Niyaz-313/trading_bot.git trading_bot

# Перейдите в директорию
cd trading_bot
```

### Вариант B: Клонирование в существующую директорию

```bash
# Перейдите в директорию
cd ~/trading_bot

# Если директория не пустая, инициализируйте Git
git init

# Добавьте remote
git remote add origin https://github.com/Niyaz-313/trading_bot.git

# Получите код
git fetch origin

# Переключитесь на ветку main
git checkout -b main origin/main
```

**Если возникли конфликты:**
```bash
# Принудительно обновите (ОСТОРОЖНО: удалит локальные изменения!)
git fetch origin
git reset --hard origin/main
```

---

## ЭТАП 4: НАСТРОЙКА GIT НА СЕРВЕРЕ

### Шаг 1: Настройка пользователя Git

```bash
# Настройте имя и email для Git
git config --global user.name "Bot Server"
git config --global user.email "bot@server.local"
```

### Шаг 2: Настройка авторизации (если репозиторий приватный)

**Вариант 1: Personal Access Token (HTTPS)**

1. На GitHub создайте Personal Access Token:
   - Settings → Developer settings → Personal access tokens → Tokens (classic)
   - Generate new token (classic)
   - Выберите права: `repo` (полный доступ к репозиториям)
   - Скопируйте токен

2. На сервере используйте токен вместо пароля:
   ```bash
   # При следующем git pull/push будет запрошен пароль
   # Используйте токен вместо пароля
   git pull origin main
   ```

**Вариант 2: SSH ключи (рекомендуется)**

1. На сервере создайте SSH ключ:
   ```bash
   ssh-keygen -t ed25519 -C "bot_server" -f ~/.ssh/id_ed25519 -N ""
   ```

2. Покажите публичный ключ:
   ```bash
   cat ~/.ssh/id_ed25519.pub
   ```

3. Добавьте ключ на GitHub:
   - GitHub → Settings → SSH and GPG keys → New SSH key
   - Вставьте публичный ключ
   - Сохраните

4. Измените remote на SSH:
   ```bash
   cd ~/trading_bot
   git remote set-url origin git@github.com:Niyaz-313/trading_bot.git
   ```

5. Проверьте подключение:
   ```bash
   ssh -T git@github.com
   ```

---

## ЭТАП 5: УСТАНОВКА ЗАВИСИМОСТЕЙ

```bash
# Перейдите в директорию
cd ~/trading_bot

# Создайте виртуальное окружение
python3 -m venv venv

# Активируйте виртуальное окружение
source venv/bin/activate

# Обновите pip
pip install --upgrade pip

# Установите зависимости
pip install -r requirements.txt
```

---

## ЭТАП 6: НАСТРОЙКА .ENV ФАЙЛА

### Вариант 1: Копирование через SCP (с локальной машины)

```powershell
# На локальной машине (PowerShell)
scp "C:\Users\karim\OneDrive\Рабочий стол\Bot\.env" botuser@YOUR_IPv4_ADDRESS:~/trading_bot/.env
```

### Вариант 2: Создание вручную на сервере

```bash
# На сервере
cd ~/trading_bot
nano .env

# Скопируйте содержимое .env с локальной машины и вставьте в nano
# Сохраните: Ctrl+O, Enter, Ctrl+X
```

### Вариант 3: Через веб-терминал провайдера

1. Откройте веб-терминал в панели управления VPS
2. Выполните:
   ```bash
   cd ~/trading_bot
   nano .env
   ```
3. Скопируйте содержимое `.env` с локального компьютера
4. Вставьте в nano и сохраните

---

## ЭТАП 7: ПРОВЕРКА РАБОТОСПОСОБНОСТИ

```bash
# На сервере
cd ~/trading_bot
source venv/bin/activate

# Проверьте подключение к T-Invest API
python3 -c "from broker_api import BrokerAPI; b = BrokerAPI(paper_trading=True); print('OK' if b.client else 'FAIL')"

# Проверьте Telegram бота
python3 -c "from telegram_bot import TelegramBot; t = TelegramBot(); print('OK')"
```

---

## ЭТАП 8: ОБНОВЛЕНИЕ КОДА НА СЕРВЕРЕ

### Ручное обновление:

```bash
# На сервере
cd ~/trading_bot
source venv/bin/activate

# Получите последние изменения
git pull origin main

# Обновите зависимости (если requirements.txt изменился)
pip install -r requirements.txt
```

### Автоматическое обновление (через cron):

```bash
# Создайте скрипт обновления
nano ~/trading_bot/update.sh
```

**Содержимое `update.sh`:**
```bash
#!/bin/bash
cd ~/trading_bot
source venv/bin/activate
git pull origin main
pip install -r requirements.txt
sudo systemctl restart trading-bot.service
```

**Сделайте исполняемым:**
```bash
chmod +x ~/trading_bot/update.sh
```

**Добавьте в cron:**
```bash
crontab -e

# Добавьте строку (обновление каждые 6 часов):
0 */6 * * * /home/botuser/trading_bot/update.sh >> /home/botuser/trading_bot/logs/update.log 2>&1
```

---

## ЭТАП 9: РАБОЧИЙ ПРОЦЕСС

### На локальной машине:

1. Внесите изменения в код
2. Закоммитьте изменения:
   ```powershell
   git add .
   git commit -m "Описание изменений"
   git push origin main
   ```

### На сервере:

1. Обновите код:
   ```bash
   cd ~/trading_bot
   git pull origin main
   ```

2. Или используйте скрипт:
   ```bash
   ~/trading_bot/update.sh
   ```

---

## 🔧 РЕШЕНИЕ ПРОБЛЕМ

### Проблема: "fatal: unable to access 'https://github.com/...': Failed to connect"

**Решение:**
- Проверьте интернет-соединение на сервере: `ping github.com`
- Если GitHub недоступен, используйте SSH вместо HTTPS:
  ```bash
  git remote set-url origin git@github.com:Niyaz-313/trading_bot.git
  ```

### Проблема: "Permission denied (publickey)"

**Решение:**
- Настройте SSH ключи (см. ЭТАП 4, Вариант 2)
- Или используйте Personal Access Token с HTTPS

### Проблема: "fatal: not a git repository"

**Решение:**
```bash
cd ~/trading_bot
git init
git remote add origin https://github.com/Niyaz-313/trading_bot.git
git fetch origin
git checkout -b main origin/main
```

---

## ✅ ЧЕКЛИСТ

- [ ] Git репозиторий создан на GitHub/GitLab
- [ ] Код отправлен на GitHub с локальной машины
- [ ] Подключение к серверу по SSH работает
- [ ] Git установлен на сервере
- [ ] Репозиторий клонирован на сервер
- [ ] Виртуальное окружение создано
- [ ] Зависимости установлены
- [ ] .env файл скопирован на сервер
- [ ] Проверка работоспособности пройдена
- [ ] Настроено автоматическое обновление

---

## 🚀 БЫСТРАЯ КОМАНДА ДЛЯ КЛОНИРОВАНИЯ

```bash
# На сервере (замените YOUR_IPv4_ADDRESS на ваш IPv4 адрес)
cd ~
git clone https://github.com/Niyaz-313/trading_bot.git trading_bot
cd trading_bot
python3 -m venv venv
source venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt
```

**Готово!** Теперь скопируйте `.env` файл и запустите бота.


## 🎯 Цель
Подключить Git репозиторий на сервере и скачать файлы из него.

---

## ЭТАП 1: ПОДГОТОВКА НА ЛОКАЛЬНОЙ МАШИНЕ

### Шаг 1: Проверка Git репозитория

```powershell
# Перейдите в директорию проекта
cd "C:\Users\karim\OneDrive\Рабочий стол\Bot"

# Проверьте статус Git
git status

# Проверьте remote репозиторий
git remote -v
```

### Шаг 2: Если репозиторий не инициализирован

```powershell
# Инициализируйте Git репозиторий
git init

# Добавьте все файлы (кроме .env и других исключений)
git add .

# Создайте первый коммит
git commit -m "Initial commit"

# Переименуйте ветку в main (если нужно)
git branch -M main
```

### Шаг 3: Настройка remote репозитория

**Если у вас уже есть репозиторий на GitHub/GitLab:**

```powershell
# Добавьте remote (если еще не добавлен)
git remote add origin https://github.com/Niyaz-313/trading_bot.git

# Или если уже есть, обновите URL:
git remote set-url origin https://github.com/Niyaz-313/trading_bot.git

# Проверьте
git remote -v
```

**Если репозитория еще нет:**

1. Создайте репозиторий на GitHub:
   - Перейдите на https://github.com/new
   - Название: `trading_bot`
   - Выберите **Private** (приватный)
   - Нажмите "Create repository"

2. Добавьте remote:
   ```powershell
   git remote add origin https://github.com/ваш_username/trading_bot.git
   ```

### Шаг 4: Отправка кода на GitHub

```powershell
# Отправьте код на GitHub
git push -u origin main
```

**Если потребуется авторизация:**
- Используйте Personal Access Token вместо пароля
- Или настройте SSH ключи для GitHub

---

## ЭТАП 2: ПОДКЛЮЧЕНИЕ К СЕРВЕРУ

### Шаг 1: Подключение по SSH

```powershell
# Подключитесь к серверу (замените YOUR_IPv4_ADDRESS на ваш IPv4 адрес)
ssh botuser@YOUR_IPv4_ADDRESS
```

**Пример:**
```powershell
ssh botuser@192.168.1.100
```

### Шаг 2: Проверка Git на сервере

```bash
# Проверьте, установлен ли Git
git --version

# Если не установлен, установите:
sudo apt update
sudo apt install -y git
```

---

## ЭТАП 3: КЛОНИРОВАНИЕ РЕПОЗИТОРИЯ НА СЕРВЕРЕ

### Вариант A: Клонирование в пустую директорию

```bash
# Перейдите в домашнюю директорию
cd ~

# Удалите старую директорию (если есть)
rm -rf trading_bot

# Клонируйте репозиторий
git clone https://github.com/Niyaz-313/trading_bot.git trading_bot

# Перейдите в директорию
cd trading_bot
```

### Вариант B: Клонирование в существующую директорию

```bash
# Перейдите в директорию
cd ~/trading_bot

# Если директория не пустая, инициализируйте Git
git init

# Добавьте remote
git remote add origin https://github.com/Niyaz-313/trading_bot.git

# Получите код
git fetch origin

# Переключитесь на ветку main
git checkout -b main origin/main
```

**Если возникли конфликты:**
```bash
# Принудительно обновите (ОСТОРОЖНО: удалит локальные изменения!)
git fetch origin
git reset --hard origin/main
```

---

## ЭТАП 4: НАСТРОЙКА GIT НА СЕРВЕРЕ

### Шаг 1: Настройка пользователя Git

```bash
# Настройте имя и email для Git
git config --global user.name "Bot Server"
git config --global user.email "bot@server.local"
```

### Шаг 2: Настройка авторизации (если репозиторий приватный)

**Вариант 1: Personal Access Token (HTTPS)**

1. На GitHub создайте Personal Access Token:
   - Settings → Developer settings → Personal access tokens → Tokens (classic)
   - Generate new token (classic)
   - Выберите права: `repo` (полный доступ к репозиториям)
   - Скопируйте токен

2. На сервере используйте токен вместо пароля:
   ```bash
   # При следующем git pull/push будет запрошен пароль
   # Используйте токен вместо пароля
   git pull origin main
   ```

**Вариант 2: SSH ключи (рекомендуется)**

1. На сервере создайте SSH ключ:
   ```bash
   ssh-keygen -t ed25519 -C "bot_server" -f ~/.ssh/id_ed25519 -N ""
   ```

2. Покажите публичный ключ:
   ```bash
   cat ~/.ssh/id_ed25519.pub
   ```

3. Добавьте ключ на GitHub:
   - GitHub → Settings → SSH and GPG keys → New SSH key
   - Вставьте публичный ключ
   - Сохраните

4. Измените remote на SSH:
   ```bash
   cd ~/trading_bot
   git remote set-url origin git@github.com:Niyaz-313/trading_bot.git
   ```

5. Проверьте подключение:
   ```bash
   ssh -T git@github.com
   ```

---

## ЭТАП 5: УСТАНОВКА ЗАВИСИМОСТЕЙ

```bash
# Перейдите в директорию
cd ~/trading_bot

# Создайте виртуальное окружение
python3 -m venv venv

# Активируйте виртуальное окружение
source venv/bin/activate

# Обновите pip
pip install --upgrade pip

# Установите зависимости
pip install -r requirements.txt
```

---

## ЭТАП 6: НАСТРОЙКА .ENV ФАЙЛА

### Вариант 1: Копирование через SCP (с локальной машины)

```powershell
# На локальной машине (PowerShell)
scp "C:\Users\karim\OneDrive\Рабочий стол\Bot\.env" botuser@YOUR_IPv4_ADDRESS:~/trading_bot/.env
```

### Вариант 2: Создание вручную на сервере

```bash
# На сервере
cd ~/trading_bot
nano .env

# Скопируйте содержимое .env с локальной машины и вставьте в nano
# Сохраните: Ctrl+O, Enter, Ctrl+X
```

### Вариант 3: Через веб-терминал провайдера

1. Откройте веб-терминал в панели управления VPS
2. Выполните:
   ```bash
   cd ~/trading_bot
   nano .env
   ```
3. Скопируйте содержимое `.env` с локального компьютера
4. Вставьте в nano и сохраните

---

## ЭТАП 7: ПРОВЕРКА РАБОТОСПОСОБНОСТИ

```bash
# На сервере
cd ~/trading_bot
source venv/bin/activate

# Проверьте подключение к T-Invest API
python3 -c "from broker_api import BrokerAPI; b = BrokerAPI(paper_trading=True); print('OK' if b.client else 'FAIL')"

# Проверьте Telegram бота
python3 -c "from telegram_bot import TelegramBot; t = TelegramBot(); print('OK')"
```

---

## ЭТАП 8: ОБНОВЛЕНИЕ КОДА НА СЕРВЕРЕ

### Ручное обновление:

```bash
# На сервере
cd ~/trading_bot
source venv/bin/activate

# Получите последние изменения
git pull origin main

# Обновите зависимости (если requirements.txt изменился)
pip install -r requirements.txt
```

### Автоматическое обновление (через cron):

```bash
# Создайте скрипт обновления
nano ~/trading_bot/update.sh
```

**Содержимое `update.sh`:**
```bash
#!/bin/bash
cd ~/trading_bot
source venv/bin/activate
git pull origin main
pip install -r requirements.txt
sudo systemctl restart trading-bot.service
```

**Сделайте исполняемым:**
```bash
chmod +x ~/trading_bot/update.sh
```

**Добавьте в cron:**
```bash
crontab -e

# Добавьте строку (обновление каждые 6 часов):
0 */6 * * * /home/botuser/trading_bot/update.sh >> /home/botuser/trading_bot/logs/update.log 2>&1
```

---

## ЭТАП 9: РАБОЧИЙ ПРОЦЕСС

### На локальной машине:

1. Внесите изменения в код
2. Закоммитьте изменения:
   ```powershell
   git add .
   git commit -m "Описание изменений"
   git push origin main
   ```

### На сервере:

1. Обновите код:
   ```bash
   cd ~/trading_bot
   git pull origin main
   ```

2. Или используйте скрипт:
   ```bash
   ~/trading_bot/update.sh
   ```

---

## 🔧 РЕШЕНИЕ ПРОБЛЕМ

### Проблема: "fatal: unable to access 'https://github.com/...': Failed to connect"

**Решение:**
- Проверьте интернет-соединение на сервере: `ping github.com`
- Если GitHub недоступен, используйте SSH вместо HTTPS:
  ```bash
  git remote set-url origin git@github.com:Niyaz-313/trading_bot.git
  ```

### Проблема: "Permission denied (publickey)"

**Решение:**
- Настройте SSH ключи (см. ЭТАП 4, Вариант 2)
- Или используйте Personal Access Token с HTTPS

### Проблема: "fatal: not a git repository"

**Решение:**
```bash
cd ~/trading_bot
git init
git remote add origin https://github.com/Niyaz-313/trading_bot.git
git fetch origin
git checkout -b main origin/main
```

---

## ✅ ЧЕКЛИСТ

- [ ] Git репозиторий создан на GitHub/GitLab
- [ ] Код отправлен на GitHub с локальной машины
- [ ] Подключение к серверу по SSH работает
- [ ] Git установлен на сервере
- [ ] Репозиторий клонирован на сервер
- [ ] Виртуальное окружение создано
- [ ] Зависимости установлены
- [ ] .env файл скопирован на сервер
- [ ] Проверка работоспособности пройдена
- [ ] Настроено автоматическое обновление

---

## 🚀 БЫСТРАЯ КОМАНДА ДЛЯ КЛОНИРОВАНИЯ

```bash
# На сервере (замените YOUR_IPv4_ADDRESS на ваш IPv4 адрес)
cd ~
git clone https://github.com/Niyaz-313/trading_bot.git trading_bot
cd trading_bot
python3 -m venv venv
source venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt
```

**Готово!** Теперь скопируйте `.env` файл и запустите бота.


## 🎯 Цель
Подключить Git репозиторий на сервере и скачать файлы из него.

---

## ЭТАП 1: ПОДГОТОВКА НА ЛОКАЛЬНОЙ МАШИНЕ

### Шаг 1: Проверка Git репозитория

```powershell
# Перейдите в директорию проекта
cd "C:\Users\karim\OneDrive\Рабочий стол\Bot"

# Проверьте статус Git
git status

# Проверьте remote репозиторий
git remote -v
```

### Шаг 2: Если репозиторий не инициализирован

```powershell
# Инициализируйте Git репозиторий
git init

# Добавьте все файлы (кроме .env и других исключений)
git add .

# Создайте первый коммит
git commit -m "Initial commit"

# Переименуйте ветку в main (если нужно)
git branch -M main
```

### Шаг 3: Настройка remote репозитория

**Если у вас уже есть репозиторий на GitHub/GitLab:**

```powershell
# Добавьте remote (если еще не добавлен)
git remote add origin https://github.com/Niyaz-313/trading_bot.git

# Или если уже есть, обновите URL:
git remote set-url origin https://github.com/Niyaz-313/trading_bot.git

# Проверьте
git remote -v
```

**Если репозитория еще нет:**

1. Создайте репозиторий на GitHub:
   - Перейдите на https://github.com/new
   - Название: `trading_bot`
   - Выберите **Private** (приватный)
   - Нажмите "Create repository"

2. Добавьте remote:
   ```powershell
   git remote add origin https://github.com/ваш_username/trading_bot.git
   ```

### Шаг 4: Отправка кода на GitHub

```powershell
# Отправьте код на GitHub
git push -u origin main
```

**Если потребуется авторизация:**
- Используйте Personal Access Token вместо пароля
- Или настройте SSH ключи для GitHub

---

## ЭТАП 2: ПОДКЛЮЧЕНИЕ К СЕРВЕРУ

### Шаг 1: Подключение по SSH

```powershell
# Подключитесь к серверу (замените YOUR_IPv4_ADDRESS на ваш IPv4 адрес)
ssh botuser@YOUR_IPv4_ADDRESS
```

**Пример:**
```powershell
ssh botuser@192.168.1.100
```

### Шаг 2: Проверка Git на сервере

```bash
# Проверьте, установлен ли Git
git --version

# Если не установлен, установите:
sudo apt update
sudo apt install -y git
```

---

## ЭТАП 3: КЛОНИРОВАНИЕ РЕПОЗИТОРИЯ НА СЕРВЕРЕ

### Вариант A: Клонирование в пустую директорию

```bash
# Перейдите в домашнюю директорию
cd ~

# Удалите старую директорию (если есть)
rm -rf trading_bot

# Клонируйте репозиторий
git clone https://github.com/Niyaz-313/trading_bot.git trading_bot

# Перейдите в директорию
cd trading_bot
```

### Вариант B: Клонирование в существующую директорию

```bash
# Перейдите в директорию
cd ~/trading_bot

# Если директория не пустая, инициализируйте Git
git init

# Добавьте remote
git remote add origin https://github.com/Niyaz-313/trading_bot.git

# Получите код
git fetch origin

# Переключитесь на ветку main
git checkout -b main origin/main
```

**Если возникли конфликты:**
```bash
# Принудительно обновите (ОСТОРОЖНО: удалит локальные изменения!)
git fetch origin
git reset --hard origin/main
```

---

## ЭТАП 4: НАСТРОЙКА GIT НА СЕРВЕРЕ

### Шаг 1: Настройка пользователя Git

```bash
# Настройте имя и email для Git
git config --global user.name "Bot Server"
git config --global user.email "bot@server.local"
```

### Шаг 2: Настройка авторизации (если репозиторий приватный)

**Вариант 1: Personal Access Token (HTTPS)**

1. На GitHub создайте Personal Access Token:
   - Settings → Developer settings → Personal access tokens → Tokens (classic)
   - Generate new token (classic)
   - Выберите права: `repo` (полный доступ к репозиториям)
   - Скопируйте токен

2. На сервере используйте токен вместо пароля:
   ```bash
   # При следующем git pull/push будет запрошен пароль
   # Используйте токен вместо пароля
   git pull origin main
   ```

**Вариант 2: SSH ключи (рекомендуется)**

1. На сервере создайте SSH ключ:
   ```bash
   ssh-keygen -t ed25519 -C "bot_server" -f ~/.ssh/id_ed25519 -N ""
   ```

2. Покажите публичный ключ:
   ```bash
   cat ~/.ssh/id_ed25519.pub
   ```

3. Добавьте ключ на GitHub:
   - GitHub → Settings → SSH and GPG keys → New SSH key
   - Вставьте публичный ключ
   - Сохраните

4. Измените remote на SSH:
   ```bash
   cd ~/trading_bot
   git remote set-url origin git@github.com:Niyaz-313/trading_bot.git
   ```

5. Проверьте подключение:
   ```bash
   ssh -T git@github.com
   ```

---

## ЭТАП 5: УСТАНОВКА ЗАВИСИМОСТЕЙ

```bash
# Перейдите в директорию
cd ~/trading_bot

# Создайте виртуальное окружение
python3 -m venv venv

# Активируйте виртуальное окружение
source venv/bin/activate

# Обновите pip
pip install --upgrade pip

# Установите зависимости
pip install -r requirements.txt
```

---

## ЭТАП 6: НАСТРОЙКА .ENV ФАЙЛА

### Вариант 1: Копирование через SCP (с локальной машины)

```powershell
# На локальной машине (PowerShell)
scp "C:\Users\karim\OneDrive\Рабочий стол\Bot\.env" botuser@YOUR_IPv4_ADDRESS:~/trading_bot/.env
```

### Вариант 2: Создание вручную на сервере

```bash
# На сервере
cd ~/trading_bot
nano .env

# Скопируйте содержимое .env с локальной машины и вставьте в nano
# Сохраните: Ctrl+O, Enter, Ctrl+X
```

### Вариант 3: Через веб-терминал провайдера

1. Откройте веб-терминал в панели управления VPS
2. Выполните:
   ```bash
   cd ~/trading_bot
   nano .env
   ```
3. Скопируйте содержимое `.env` с локального компьютера
4. Вставьте в nano и сохраните

---

## ЭТАП 7: ПРОВЕРКА РАБОТОСПОСОБНОСТИ

```bash
# На сервере
cd ~/trading_bot
source venv/bin/activate

# Проверьте подключение к T-Invest API
python3 -c "from broker_api import BrokerAPI; b = BrokerAPI(paper_trading=True); print('OK' if b.client else 'FAIL')"

# Проверьте Telegram бота
python3 -c "from telegram_bot import TelegramBot; t = TelegramBot(); print('OK')"
```

---

## ЭТАП 8: ОБНОВЛЕНИЕ КОДА НА СЕРВЕРЕ

### Ручное обновление:

```bash
# На сервере
cd ~/trading_bot
source venv/bin/activate

# Получите последние изменения
git pull origin main

# Обновите зависимости (если requirements.txt изменился)
pip install -r requirements.txt
```

### Автоматическое обновление (через cron):

```bash
# Создайте скрипт обновления
nano ~/trading_bot/update.sh
```

**Содержимое `update.sh`:**
```bash
#!/bin/bash
cd ~/trading_bot
source venv/bin/activate
git pull origin main
pip install -r requirements.txt
sudo systemctl restart trading-bot.service
```

**Сделайте исполняемым:**
```bash
chmod +x ~/trading_bot/update.sh
```

**Добавьте в cron:**
```bash
crontab -e

# Добавьте строку (обновление каждые 6 часов):
0 */6 * * * /home/botuser/trading_bot/update.sh >> /home/botuser/trading_bot/logs/update.log 2>&1
```

---

## ЭТАП 9: РАБОЧИЙ ПРОЦЕСС

### На локальной машине:

1. Внесите изменения в код
2. Закоммитьте изменения:
   ```powershell
   git add .
   git commit -m "Описание изменений"
   git push origin main
   ```

### На сервере:

1. Обновите код:
   ```bash
   cd ~/trading_bot
   git pull origin main
   ```

2. Или используйте скрипт:
   ```bash
   ~/trading_bot/update.sh
   ```

---

## 🔧 РЕШЕНИЕ ПРОБЛЕМ

### Проблема: "fatal: unable to access 'https://github.com/...': Failed to connect"

**Решение:**
- Проверьте интернет-соединение на сервере: `ping github.com`
- Если GitHub недоступен, используйте SSH вместо HTTPS:
  ```bash
  git remote set-url origin git@github.com:Niyaz-313/trading_bot.git
  ```

### Проблема: "Permission denied (publickey)"

**Решение:**
- Настройте SSH ключи (см. ЭТАП 4, Вариант 2)
- Или используйте Personal Access Token с HTTPS

### Проблема: "fatal: not a git repository"

**Решение:**
```bash
cd ~/trading_bot
git init
git remote add origin https://github.com/Niyaz-313/trading_bot.git
git fetch origin
git checkout -b main origin/main
```

---

## ✅ ЧЕКЛИСТ

- [ ] Git репозиторий создан на GitHub/GitLab
- [ ] Код отправлен на GitHub с локальной машины
- [ ] Подключение к серверу по SSH работает
- [ ] Git установлен на сервере
- [ ] Репозиторий клонирован на сервер
- [ ] Виртуальное окружение создано
- [ ] Зависимости установлены
- [ ] .env файл скопирован на сервер
- [ ] Проверка работоспособности пройдена
- [ ] Настроено автоматическое обновление

---

## 🚀 БЫСТРАЯ КОМАНДА ДЛЯ КЛОНИРОВАНИЯ

```bash
# На сервере (замените YOUR_IPv4_ADDRESS на ваш IPv4 адрес)
cd ~
git clone https://github.com/Niyaz-313/trading_bot.git trading_bot
cd trading_bot
python3 -m venv venv
source venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt
```

**Готово!** Теперь скопируйте `.env` файл и запустите бота.




