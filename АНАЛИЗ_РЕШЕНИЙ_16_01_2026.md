# АНАЛИЗ РЕШЕНИЙ БОТА О ПОКУПКЕ ЗА 16 ЯНВАРЯ 2026

## 📊 ОСНОВНАЯ СТАТИСТИКА

**Период анализа:** 2026-01-16 00:00:00 UTC - текущее время

### Ключевые показатели:

- **Решений (decision):** ~1000+ (за весь день)
- **Пропусков (skip):** 0 (нет событий skip)
- **Покупок (trade BUY):** 0 ❌
- **Решений с signal='buy':** 1 (SNGSP)
- **Решений с strategy_should_buy=true:** 0 ❌

## 🔍 ДЕТАЛЬНЫЙ АНАЛИЗ

### КРИТИЧЕСКИЙ СЛУЧАЙ: SNGSP

**Время:** 2026-01-16 15:28:39 UTC  
**Цикл:** 28

**Параметры:**
- `signal: "buy"` ✅
- `confidence: 0.9` ⚠️ **ОЧЕНЬ ВЫСОКИЙ!**
- `buy_signals: 4` ⚠️ **МАКСИМУМ!**
- `RSI: 29.93` ⚠️ **СИЛЬНАЯ ПЕРЕПРОДАННОСТЬ!**
- `trend: "sideways"`
- `macd_hist: -0.019` (отрицательный)
- `price: 42.77`

**НО:**
- `strategy_should_buy: false` ❌ **БЛОКИРОВАНО!**

**Причина блокировки:**
Фильтр в `should_buy()` блокирует покупки в sideways при отрицательном MACD_hist, даже при сильной перепроданности (RSI < 30).

**Код блокировки (trading_strategy.py, строки 261-268):**
```python
if trend == 'sideways' and macd_hist is not None:
    try:
        if float(macd_hist) < 0:
            # Разрешаем покупку в боковике только при заметной перепроданности.
            if rsi is None or pd.isna(rsi) or float(rsi) > 35:
                return False
```

**Проблема:** Логика разрешает покупку при RSI < 35, но проверка происходит ДО этого условия, и функция возвращает False раньше.

## 📈 АНАЛИЗ ВСЕХ РЕШЕНИЙ "НЕ ПОКУПАТЬ"

### Причины отказа от покупки:

1. **Sideways + отрицательный MACD:** ~40% решений
   - Примеры: SNGSP, CHMF, BANEP
   - Проблема: Блокирует даже при сильной перепроданности

2. **Trend = down:** ~30% решений
   - Примеры: PLTRUB_TOM, TRNFP, CNYRUB_TOM
   - Обоснованно: Не покупаем против тренда

3. **RSI слишком высокий (>68):** ~20% решений
   - Примеры: SBER (RSI=71-72), GAZP (RSI=67-72), VTBR (RSI=70), MOEX (RSI=70), LKOH (RSI=73), NVTK (RSI=81)
   - Обоснованно: Правильно не покупаем на перекупленности

4. **Низкий confidence:** ~10% решений
   - Большинство решений имеют confidence=0.0
   - Проблема: Confidence не рассчитывается для сигналов, которые не проходят should_buy()

## 💰 УПУЩЕННЫЕ ВОЗМОЖНОСТИ

### 1. SNGSP - КРИТИЧЕСКАЯ УПУЩЕННАЯ ВОЗМОЖНОСТЬ

- **Время:** 2026-01-16 15:28:39 UTC
- **Цена решения:** 42.77
- **Confidence:** 0.9 (максимальный!)
- **Buy signals:** 4 (максимум!)
- **RSI:** 29.93 (сильная перепроданность)
- **Причина блокировки:** Sideways + negative MACD
- **Оценка:** Это была бы отличная покупка (mean-reversion на сильной перепроданности)

**Рекомендация:** Исправить логику в `should_buy()` для разрешения покупок при RSI < 30 в sideways, даже при отрицательном MACD.

## 🔧 РЕКОМЕНДАЦИИ ПО ИСПРАВЛЕНИЮ

### 1. КРИТИЧНО: Исправить логику should_buy() для SNGSP

**Проблема:** Логика проверяет `rsi > 35` ДО разрешения покупки при перепроданности.

**Текущий код:**
```python
if trend == 'sideways' and macd_hist is not None:
    try:
        if float(macd_hist) < 0:
            # Разрешаем покупку в боковике только при заметной перепроданности.
            if rsi is None or pd.isna(rsi) or float(rsi) > 35:
                return False
```

**Исправление:**
```python
if trend == 'sideways' and macd_hist is not None:
    try:
        if float(macd_hist) < 0:
            # Разрешаем покупку в боковике при сильной перепроданности (RSI < 30)
            # или при высоком confidence (>0.8)
            if rsi is not None and not pd.isna(rsi):
                if float(rsi) <= 30:
                    # Сильная перепроданность - разрешаем покупку
                    pass  # Продолжаем проверку
                elif float(rsi) > 35:
                    return False
            else:
                return False
```

**Или более простое решение:**
```python
if trend == 'sideways' and macd_hist is not None:
    try:
        if float(macd_hist) < 0:
            # Разрешаем покупку при сильной перепроданности (RSI <= 30)
            if rsi is None or pd.isna(rsi) or float(rsi) > 30:  # Изменено с 35 на 30
                return False
```

### 2. Улучшить расчет confidence

**Проблема:** Confidence = 0.0 для большинства решений, даже когда есть хорошие сигналы.

**Рекомендация:** Рассчитывать confidence ДО проверки should_buy(), чтобы видеть потенциал сигнала даже если он блокируется.

### 3. Добавить логирование причин блокировки

**Проблема:** Нет событий skip, поэтому непонятно, почему покупки блокируются.

**Рекомендация:** Добавить skip events с указанием конкретной причины блокировки (например, "sideways_negative_macd_rsi_ok", "rsi_too_high", и т.д.)

## 📊 СТАТИСТИКА ПО СИМВОЛАМ

### Символы с потенциально хорошими сигналами (но заблокированы):

1. **SNGSP:** RSI=29.93, confidence=0.9, 4 buy_signals ❌ БЛОКИРОВАН
2. **RUAL:** RSI=36.86, trend=up, но confidence=0.0
3. **CHMF:** RSI=39.60, trend=sideways, но negative MACD
4. **SLVRUB_TOM:** RSI=40.19, positive MACD_hist, но trend=down

### Символы в перекупленности (правильно не покупаются):

- **SBER:** RSI=71-72 ✅
- **GAZP:** RSI=67-72 ✅
- **VTBR:** RSI=70 ✅
- **MOEX:** RSI=70 ✅
- **LKOH:** RSI=73 ✅
- **NVTK:** RSI=81 ✅

## ✅ ВЫВОДЫ

1. **Основная проблема:** Логика `should_buy()` блокирует покупку SNGSP с отличными параметрами (confidence=0.9, RSI=29.93, 4 buy_signals) из-за проверки `rsi > 35` вместо `rsi > 30`.

2. **Обоснованные отказы:** Большинство отказов обоснованы (перекупленность, down trend).

3. **Упущенная возможность:** SNGSP - это явная упущенная возможность для mean-reversion покупки.

4. **Нет событий skip:** Проблема на уровне стратегии, а не фильтров бота.

## 🎯 ПРИОРИТЕТНЫЕ ДЕЙСТВИЯ

1. **СРОЧНО:** Исправить проверку RSI в `should_buy()` с `> 35` на `> 30` для разрешения покупок при сильной перепроданности в sideways.

2. **ВАЖНО:** Добавить исключение для случаев с очень высоким confidence (>0.8) - разрешать покупку даже при отрицательном MACD в sideways.

3. **РЕКОМЕНДУЕТСЯ:** Улучшить расчет confidence для всех сигналов, не только прошедших should_buy().

4. **РЕКОМЕНДУЕТСЯ:** Добавить логирование причин блокировки в skip events.

---

**Дата анализа:** 2026-01-16  
**Файл аудита:** audit_logs/trades_audit.jsonl  
**Последнее событие:** 2026-01-16 15:34:xx UTC


