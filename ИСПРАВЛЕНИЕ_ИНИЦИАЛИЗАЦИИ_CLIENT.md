# ✅ Исправление инициализации Client

## ❌ Проблема

Ошибка: `Client.__init__() got an unexpected keyword argument 'sandbox'`

## ✅ Решение

Официальный SDK использует параметр `target`, а не `sandbox`.

**Правильная инициализация:**

```python
# Для песочницы
client = Client(token=token, target='sandbox')

# Для продакшн
client = Client(token=token)
```

## 📋 Что исправлено

В файле `tinvest_api.py`:
- Заменен параметр `sandbox=sandbox` на `target='sandbox'` для песочницы
- Для продакшн не указывается target (используется по умолчанию)

## ✅ Проверка

После исправления клиент должен инициализироваться правильно.

Запустите: `python backtest.py`

Все должно работать!







